---
title: "Dreamer-CPC: Message Learning with World Models for Decentralized Multi-agent Reinforcement Learning"
title_zh: Dreamer-CPC：基于世界模型的消息学习用于去中心化多智能体强化学习
authors: "Taisuke Takayama, Naoto Yoshida, Tadahiro Taniguchi"
date: 2026-07-22
pdf: "https://arxiv.org/pdf/2607.19809v1"
tags: ["query:agent-memory"]
score: 9.0
evidence: 提出一种基于世界模型的去中心化多智能体强化学习方法
tldr: 在多智能体强化学习中，部分可观测场景下通信能提升性能，但现有方法仅基于当前观测生成消息，忽略历史信息。Dreamer-CPC将集体预测编码消息学习融入DreamerV3世界模型，使智能体从潜在状态推断并交换历史感知消息。在CatchApple等任务上，该方法大幅超越基于当前观测的IPPO-CPC，回报提升4-5倍，并成功协调其他方法失效的场景。研究成果表明，结合世界模型潜在动态的消息能有效弥补观测缺失，促进去中心化决策。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.19809v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1268, \"height\": 495, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19809v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1274, \"height\": 556, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19809v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1271, \"height\": 551, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19809v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 453, \"height\": 345, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19809v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1259, \"height\": 627, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19809v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1268, \"height\": 621, \"label\": \"Figure\"}]"
motivation: 现有通信方法仅从当前观测生成消息，难以利用历史信息，在观测缺失时性能受限。
method: 将CPC消息模块嵌入DreamerV3世界模型，智能体独立维护世界模型并发掘潜在状态中的历史信息进行消息传递。
result: Dreamer-CPC在CatchApple任务中比IPPO-CPC回报提升4-5倍，且能处理观测缺失下的协调问题。
conclusion: 基于世界模型潜在动态的消息学习可在观测不足时支持去中心化多智能体有效协作。
---

## 摘要
在多智能体强化学习（MARL）中，智能体间通信对于提高部分可观测下的性能是有效的。基于表示学习的方法使去中心化智能体能够基于自身观测学习消息，但它们仅依赖当前观测，无法传递随时间积累的信息。我们提出 Dreamer-CPC，一种基于模型的去中心化 MARL 方法，它将基于集体预测编码（CPC）的消息学习整合到 DreamerV3 的世界模型中。每个智能体独立维护一个世界模型和一个消息模块，并从反映过去观测和动作历史的潜在状态中推断和交换消息。我们在两个环境中评估了 Dreamer-CPC：Observer，一个非合作信息共享任务；和 CatchApple，一个新提出的任务相关观测暂时缺失的任务。在两个环境中，Dreamer-CPC 都优于基于 CPC 的现有方法 IPPO-CPC（其从当前观测生成消息）以及无通信基线。特别是在 CatchApple 中，Dreamer-CPC 的每回合回报达到 IPPO-CPC 的 4 到 5 倍，展示了在其他方法因观测缺失而失败时的有效协调能力。这些结果表明，当仅依靠当前观测不足时，基于世界模型潜在动态的通信可以为去中心化决策提供支持。

## Abstract
In multi-agent reinforcement learning (MARL), inter-agent communication is effective for improving performance under partial observability. Representation learning-based approaches enable decentralized agents to learn messages grounded in their own observations, but they rely only on current observations and cannot convey information accumulated over time. We propose Dreamer-CPC, a decentralized model-based MARL method that integrates message learning based on Collective Predictive Coding (CPC) into the world model of DreamerV3. Each agent independently maintains a world model and a message module, and infers and exchanges messages from the latent states of the world model that reflect the history of past observations and actions. We evaluated Dreamer-CPC in two environments: Observer, a non-cooperative information-sharing task, and CatchApple, a newly introduced task in which task-relevant observations are temporarily missing. In both environments, Dreamer-CPC outperformed IPPO-CPC, an existing CPC-based method that generates messages from current observations, as well as no-communication baselines. In particular, in CatchApple, Dreamer-CPC achieved 4 to 5 times the episode return of IPPO-CPC, demonstrating effective coordination where other methods fail due to missing observations. These results suggest that communication grounded in the latent dynamics of world models can support decentralized decision-making when current observations alone are insufficient.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在部分可观测的去中心化多智能体系统中，智能体通常只能基于自身局部历史信息做决策，而任务相关信息可能分散在多个智能体之间。虽然通信可以缓解这个问题，但现有的基于表示学习的通信方法（如 CPC 类方法）**只从当前观测生成消息**，无法传递随历史积累的预测性信息。当任务相关观测暂时遮挡或需要预测未来状态时，仅仅依靠当前观测进行通信就不足以支撑有效协调。
- **整体含义**：本文提出将消息学习与其世界模型的潜在动态相结合，使得消息携带的信息不再局限于即时观测，而是基于智能体对环境的时序建模和状态推断，从而提升在观测缺失或需要预测的场景下的多智能体协调性能。

---

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程（用文字说明）

- **核心思想**：每个智能体独立维护一个基于**循环状态空间模型（RSSM）** 的世界模型，以及一个**循环消息模块**。消息从世界模型的潜在状态（综合了历史观测和动作）中推断产生，而非直接从原始观测生成，使通信内容蕴含时序上下文信息和预测性信息。
- **关键技术细节**：
  - **世界模型**：采用 DreamerV3 的 RSSM 结构，包含确定性循环状态 \( h_t^k \) 和随机潜在状态 \( z_t^k \)，通过式 \( h_t^k = f_{\phi^k}(h_{t-1}^k, z_{t-1}^k, a_{t-1}^k, \hat{m}_t) \) 引入其他智能体的预测消息 \( \hat{m}_t \)，并在观测重建时加入所有智能体的后验消息 \( m_t \)。
  - **消息模块**：每个智能体有一个循环状态 \( \xi_t^k \)，其更新依赖于上一时间步所有智能体的消息 \( m_{t-1} \)。消息先验 \( p_{\psi^k}(m_t^k | \xi_t^k) \) 仅由历史消息序列生成，消息后验 \( q_{\psi^k}(m_t^k | \xi_t^k, h_t^k, z_t^k) \) 进一步结合当前世界模型隐状态。
  - **联合优化**：世界模型与消息模块通过**统一的变分下界目标函数**进行端到端训练，并引入 KL-balance 和 free bits 来稳定训练。公式（19）-（24）定义了包含预测损失、动力学 KL、表示 KL 以及消息先验-后验 KL 的损失函数。
  - **去中心化实现**：其他智能体传来的消息使用停止梯度（stop gradient），防止梯度在不同智能体策略间流动，保持去中心化训练的特性。
  - **策略优化**：沿用 DreamerV3 的想象轨迹（imagination rollout）进行 actor-critic 学习，不同之处在于每一步想象中需先利用消息模块计算和交换消息，再更新 RSSM 状态，然后计算 actor 和 critic 损失。

- 算法流程（图 2 和算法 1、2）主要步骤：
  1. 从回放缓冲中采样局部轨迹，智能体依次通过消息模块产生先验消息 \( \hat{m}_t^k \)，结合其他智能体先验消息（停止梯度）更新 RSSM 状态，再通过后验推理获得 \( z_t^k \) 和 \( m_t^k \)，并预测观测/奖励/继续标志。
  2. 计算式（19）定义的损失更新世界模型和消息模块参数。
  3. 在想象轨迹中，重复消息推理和交换过程，在潜在空间内生成多步 rollout，然后用 DreamerV3 原有的 actor-critic 损失更新策略网络。

---

### 3. 实验设计：使用了哪些数据集/场景，它的 benchmark 是什么，对比了哪些方法

- **实验场景**：
  1. **Observer 环境**：两个智能体，一个智能体固定并观测奖励位置（但不能移动），另一个智能体可移动但不能直接观测奖励位置，必须通过通信获得信息。该环境测试无共享奖励下的信息传递能力。
  2. **CatchApple 环境**：新提出的环境，模拟掉落物体预测捕捉。Agent1 观测物体轨迹，Agent2 控制捕捉器但只能在物体到达最底行后才能直接看到列位置，因此必须利用通信提前预测物体落点并移动到对应列。该环境专门考察观测临时遮挡时，是否需要基于时序预测进行消息学习。
- **Benchmark 与对比方法**：
  - 基于当前观测的 CPC 通信方法：**IPPO-CPC**（独立 PPO + CPC 消息模块）。
  - 无通信基线：**IPPO**（无通信）和独立 **DreamerV3（no-comm）**。
  - 上限基线：DreamerV3 **共享观测版本（obs-shared）**。
  - 对 IPPO-CPC 和 Dreamer-CPC 分别测试了两种消息格式：单个 20-way one-hot 向量（20×1）和 4 个独立的 5-way one-hot 向量（5×4）。

---

### 4. 资源与算力：如果文中有提到，请总结使用了多少算力（GPU 型号、数量、训练时长等）。若未明确说明，也请指出这一点

- 文中**未明确说明**使用的 GPU 型号、数量、训练时间等具体算力信息。仅在模型大小上提到每个智能体的 RSSM 和 actor-critic 网络约有 3M 参数，消息模块使用了 BlockGRU 和两层 MLP，但未给出硬件配置和训练时间。

---

### 5. 实验数量与充分性：大概做了多少组实验（如不同数据集、消融实验等），这些实验是否充分、是否客观、公平

- **实验组数**：
  - 在 2 个环境中各测试了以下方法：IPPO、IPPO-CPC（2 种消息格式）、DreamerV3（no-comm）、DreamerV3（obs-shared）、Dreamer-CPC（2 种消息格式），共 **6 种配置/方法 × 2 环境 = 12 组（不含重复测量）**。
  - 每组进行 10 次独立运行，报告了 IQM 和 95%置信区间。
- **充分性与公平性**：
  - 对照设计合理：包含无通信基准、基于当前观测的通信方法、理想通信上限，能够清晰区分世界模型消息学习带来的增益。
  - 未进行消融实验（如移除世界模型而仅保留时序消息编码、改变消息模块结构等），也未在更复杂或更广泛的标准 MARL 基准上测试。因此，实验范围稍显局限，但结果具有初步可信性。
  - 实验在同一环境下采用相同训练步数，方法间比较公平。

---

### 6. 论文的主要结论与发现

- 在 Observer 中，Dreamer-CPC 在无直接观测共享的条件下显著超越 IPPO-CPC 及无通信基线，说明**基于世界模型的消息学习在非合作信息共享任务中更有效**。
- 在 CatchApple 中，Dreamer-CPC 的每回合回报达到 IPPO-CPC 的 4-5 倍，而 IPPO-CPC 几乎与无通信性能持平，证明了当**观测暂时缺失、需要预测时**，仅仅依赖当前观测产生消息是不够的，而结合潜在动态的通信可以支撑成功协调。
- 结论：将消息学习建立在世界模型的时序潜在状态之上，能够弥补部分可观测性或观测延迟造成的决策困难，支持去中心化多智能体有效协调。

---

### 7. 优点：方法或实验设计上有哪些亮点

- **方法创新**：首次将 CPC 消息学习融入基于 RSSM 的世界模型，使通信内容不再局限于即时观测，而是包含环境动态和时序上下文，解决了观测临时缺失下的协调难题。
- **去中心化实现精巧**：通过对其他智能体消息使用 stop gradient 保持独立学习，同时结合消息先验-后验推理和联合变分目标，实现统一的端到端训练。
- **新环境设计**：CatchApple 环境针对性地评估了需要预测未来状态和观测短期遮挡情况下的通信作用，为后续研究提供了一个有意义的测试平台。
- **结果显著**：在关键任务上，Dreamer-CPC 相对基线提升幅度很大，尤其是 CatchApple 的表现，有力支撑了论点。

---

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **缺乏对消息内容的分析**：文中未对学习到的消息进行定性或定量分析，如消息是否编码了未来落点、历史轨迹等，理解机制不深。
- **实验规模有限**：
  - 仅在 2 个相对简单的双智能体定制环境中测试，未在更多智能体数量或更通用的 MARL 基准（如 SMAC、MPE）上验证。
  - 没有进行消融实验，如去除世界模型时序建模能力，或仅对当前观测编码后送入消息模块等，无法区分各部分贡献。
- **训练成本和资源未报告**：无法评估该方法的实际开销和部署可行性。
- **非合作但无对抗**：实验都是非合作（各自目标）但不存在显式对抗，限制了方法在竞争性场景下的适用性论证。
- **消息表示形式较简单**：虽然测试了两种离散格式，但未探索连续消息或更丰富的消息结构。

（完）
