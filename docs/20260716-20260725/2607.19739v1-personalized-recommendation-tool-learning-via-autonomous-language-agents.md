---
title: Personalized Recommendation Tool Learning via Autonomous Language Agents
title_zh: 通过自主语言代理实现个性化推荐工具学习
authors: "Mingdai Yang, Zhiwei Liu, Weizhi Zhang, Yibo Wang, Hao Peng, Philip Yu"
date: 2026-07-22
pdf: "https://arxiv.org/pdf/2607.19739v1"
tags: ["query:agent-memory"]
score: 9.0
evidence: 提出基于记忆的智能体框架克服推荐中LLM的局限性
tldr: 针对现有基于大语言模型（LLM）的推荐系统受限于幻觉和上下文长度，无法直接应用于全排序推荐的问题，本文提出PRTA框架，通过LLM智能体作为中央规划器，调用多个传统推荐模型作为工具，并利用反思机制根据用户画像和候选列表进行个性化工具选择。在三个公开数据集上的实验表明，PRTA在提升推荐性能方面显著优于传统模型和LLM基线。该工作通过架构创新规避LLM缺陷，融合推理与扩展性，为个性化推荐提供新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.19739v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1557, \"height\": 802, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19739v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 853, \"height\": 262, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19739v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 859, \"height\": 277, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.19739v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1748, \"height\": 616, \"label\": \"Table\"}]"
motivation: 现有基于LLM的推荐模型面临幻觉与上下文长度限制，难以胜任全排序推荐任务，需架构创新。
method: 提出PRTA框架，以LLM为智能体中央规划器，与多个传统推荐工具交互，并通过反思机制实现个性化工具选择。
result: 在三个公开数据集上，PRTA显著超越传统推荐模型和LLM基线，提升全排序推荐效果。
conclusion: PRTA通过架构设计规避LLM局限，融合推理与扩展性，为个性化推荐提供高效新范式。
---

## 摘要
尽管大语言模型（LLM）因其强大的推理能力和广泛的世界知识而近期在推荐系统中受到关注，但以往基于LLM的代理存在幻觉和上下文长度限制的问题，因此不适合全量排序推荐任务。为了通过架构设计而非修改LLM本身来规避这些限制，我们提出了一种基于代理的推荐框架，即基于记忆的个性化推荐工具学习通过自主语言代理（PRTA），其中LLM作为中央规划器与多个推荐模型作为工具进行交互。基于LLM的代理负责高层推理和个性化工具选择，而传统的推荐模型则利用其在建模行为模式方面的可扩展性来执行全量排序评分。为了支持个性化工具选择，我们设计了反思机制，使代理能够根据用户画像和候选排序列表为每个用户评估和比较工具。在三个公开数据集上的大量实验表明，\modelname 在提升全量排序推荐性能方面优于传统推荐和基于LLM的基线方法。

## Abstract
Although large language models (LLMs) have recently gained traction in recommender systems due to their strong reasoning capabilities and extensive world knowledge, previous LLM-based agents suffer from hallucination and context-length limitations, and thus are not suitable for full-ranking recommendation tasks. To circumvent these limitations through architectural design rather than modifying the LLM itself, we propose an agent-based recommendation framework, memory-based $\textbf{P}$ersonalized $\textbf{R}$ecommendation $\textbf{T}$ool learning via autonomous language $\textbf{A}$gents (PRTA), in which an LLM acts as a central planner interacting with multiple recommendation models as tools. The LLM-based agent is responsible for high-level reasoning and personalized tool selection, while traditional recommendation models perform full-ranking scoring, leveraging their scalability in modeling behavioral patterns. To support personalized tool selection, we design reflection mechanisms that enable the agent to evaluate and compare tools for each user based on user profiles and candidate ranked lists. Extensive experiments across three public datasets demonstrate the superiority of \modelname over traditional recommendation and LLM-based baselines in improving full-ranking recommendation performance.

---

## 论文详细总结（自动生成）

好的，以下是根据您提供的论文内容生成的结构化中文总结。

### 1. 论文的核心问题与整体含义

*   **研究动机**：大语言模型（LLM）因具备强大的推理能力和世界知识而被用于推荐系统，但现存基于LLM的代理方法面临两大核心限制：
    *   **幻觉问题**：LLM可能生成无法映射到有效物品ID的不存在物品。
    *   **上下文长度限制**：LLM无法对整个物品目录进行全量排序，只能在小范围候选物品中进行评估，这与实际推荐需求不符。
*   **整体含义**：论文提出一种架构创新思路，**不直接修改LLM本身来解决其内在缺陷**，而是将LLM作为中央“规划器”，通过调用多个专门的传统推荐模型作为“工具”来完成推荐。这旨在将LLM的语义推理优势与传统模型的行为建模和全量排序能力相结合，从而规避LLM的上述局限，实现高效、可靠的全量排序推荐。

### 2. 论文提出的方法论

*   **核心思想**：提出基于记忆的个性化推荐工具学习框架 PRTA。该框架解耦了行为模式建模与语义推理，LLM代理负责高层推理和个性化工具选择，而传统推荐模型则负责执行全量排序。
*   **关键技术细节**：
    *   **用户画像生成**：利用LLM根据用户的历史交互物品文本描述，生成结构化的用户偏好摘要（用户画像）。
    *   **个性化工具记忆**：为每个用户维护一个记忆向量，存储每个推荐工具（如LightGCN, SASRec, SimpleX）的适用性权重。最终推荐列表通过加权聚合各工具的排序分数生成。
    *   **代理反思机制**：这是PRTA的核心，用于优化个性化工具记忆，包含三个模块：
        1.  **局部工具评估**：LLM代理根据用户画像和历史行为，独立评估每个工具的Top-K推荐列表与用户近期行为的相关性，并据此更新该工具的记忆权重。
        2.  **全局工具比较**：LLM代理比较所有工具的Top-K推荐列表，选择一个与用户近期行为最相关的工具，并增加其记忆权重。
        3.  **排序比较**：基于用户近期交互物品在各工具推荐列表中的实际排名，通过公式计算并更新工具权重，作为对LLM文本判断的补充信号。
    *   **上下文重排序**：在推理阶段，LLM代理基于用户画像，对聚合后得分最高的少量候选物品进行语义层面的细粒度重排序，生成最终推荐列表。

*   **算法流程**：
    1.  **训练阶段**：对每个用户，首先调用LLM生成用户画像。然后，在多个训练轮次中，依次执行局部工具评估、全局工具比较和排序比较三个反思模块，迭代更新个性化工具记忆中的工具权重。
    2.  **推理阶段**：利用优化后的个性化工具记忆，加权聚合所有工具的排序分数。最后，对聚合结果的Top-物品进行上下文重排序，得到最终的推荐列表。

### 3. 实验设计

*   **数据集**：在三个具有时间戳的公开数据集上进行了评估，任务为**序列推荐**中的下一项预测。
    *   **Amazon** (电影和CD类目)
    *   **Yelp** (商户)
    *   **Goodreads** (书籍)
*   **对比方法**：实验对比了三大类基线模型，以体现性能提升的全面性。
    *   **传统推荐模型**：LightGCN, SASRec, SimpleX, ENMF, DiffRec, FEARec。
    *   **纯语言/LLM基线**：BM25, LLMRanker。
    *   **检索增强生成（RAG）变体**：LightGCN_RAG, SASRec_RAG, SimpleX_RAG（即使用LLM对单个工具的Top-50结果进行重排序）。
*   **评估指标**：采用全排序场景下的标准指标：Recall@10, 20 和 NDCG@10, 20。

### 4. 资源与算力

*   **LLM型号与部署**：采用**量化版本的Phi-4**模型作为骨干LLM，并通过vLLM在本地部署。
*   **硬件与效率**：在使用优化后的工具记忆进行推理时，在一块 **V100-32GB GPU** 上，为每个用户生成最终重排列表只需不到 **0.5秒**。
*   **成本控制**：文中明确提到，由于每个用户的记忆是独立优化的，该阶段可以“令人尴尬地并行”，并能线性扩展。实验中每个数据集仅随机抽取160个用户进行LLM代理的优化与评估，此举是为了**控制LLM API调用的成本**，而非框架固有的扩展性限制。

### 5. 实验数量与充分性

*   **实验数量**：
    *   **主实验**：1组（在3个数据集上，PRTA与11种基线方法进行性能对比）。
    *   **消融实验**：1组（在3个数据集上，逐步移除PRTA的关键LLM模块，以验证各组件的贡献，共4种变体）。
    *   **可视化分析**：1组（对学习到的个性化工具记忆权重分布进行可视化）。
*   **充分性评估**：实验设计相对充分。
    *   **客观性**：使用了三个领域的公开数据集，与多样化的基线（传统模型、LLM方法、RAG变体）进行对比，比较全面。
    *   **公平性**：明确指出多数过往LLM代理工作因无法执行全量排序而被排除在外，避免了不公平的性能对比。消融实验严谨地论证了各模块的作用。

### 6. 论文的主要结论与发现

*   PRTA在所有三个数据集上均取得最佳的全排序推荐性能，验证了通过架构设计结合传统模型与LLM代理的有效性。
*   LLM代理在语义丰富的场景（如Amazon）中对重排序的提升效果更显著；而在文本信息较弱的场景（如Yelp），局部工具评估模块的贡献更大。
*   仅对单一检索工具的结果进行RAG增强，效果不稳定且可能降低性能，而PRTA聚合多工具信号后再进行重排序，鲁棒性更强。
*   序列推荐模型（SASRec）得到的记忆权重分布偏向于1，而矩阵分解模型（SimpleX）偏向于0，表明LLM代理能够根据用户行为有效甄别并选择更合适的推荐范式。

### 7. 优点

*   **创新的架构设计**：巧妙地通过“LLM作为规划器，传统模型作为执行器”的代理框架，规避了LLM的幻觉和上下文限制，而非试图解决这些基础模型难题。
*   **结合了两种范式优势**：成功融合了LLM的语义推理与传统模型在交互信号建模和大规模排序上的可扩展性。
*   **可解释的决策过程**：个性化工具记忆提供了直观的权重分配，使得代理为何偏好某种工具变得可解释。
*   **反思机制设计周密**：结合了独立的局部评估、对比的全局评估和客观的排序性能反馈，从多角度优化工具选择，提升了可靠性。

### 8. 不足与局限

*   **工具集的局限性**：实验仅采用了三种传统推荐模型（LightGCN, SASRec, SimpleX）作为工具。这虽然保证了评估的简洁性，但未能展示框架在集成更复杂、更多样化推荐工具（如基于知识图谱、深度学习的复杂模型）时的潜力。
*   **延迟与成本问题**：尽管作者提出推理速度尚可，也通过采样用户来控制训练成本，但LLM调用本身（尤其是在反思训练阶段）仍比纯传统推荐模型引入了更高的计算延迟与API开销，这可能限制其在超低延迟在线场景的应用。
*   **LLM依赖性**：框架的性能高度依赖于底层LLM的指令遵循和推理能力。使用小规模、低能力的LLM可能会影响反思判断和重排序的质量。
*   **评估设置简化**：每个数据集仅对160个用户进行LLM代理的优化和评估，虽然作为成本控制策略可以理解，但可能无法完全反映大规模用户群下的性能波动和泛化能力。

（完）
