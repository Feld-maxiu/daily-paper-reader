---
title: Just A Rather Very Intelligent Spoken Agent
title_zh: 只是一个相当非常智能的语音智能体
authors: "Chen Chen, Zhehuai Chen"
date: 2026-07-18
pdf: "https://arxiv.org/pdf/2607.16610v1"
tags: ["query:agent-memory"]
score: 8.0
evidence: 口语智能体中介实现与用户的实时多模态交互
tldr: 长视界AI智能体在复杂任务中自主运行，但用户常失去对过程的感知与干预机会。针对这一缺口，本文提出JarvisBench基准，含智能体协作与用户交互两个评测赛道，量化语音中介的价值。基于参考原型在34个文本任务上，使用GPT-5.5、Claude Opus 4.7等多种LLM智能体实验，结果显示中介能根据执行轨迹回答用户查询，并在适当节点注入稀疏指导后提升任务完成率；交互实验也表明中介改善了执行过程的可解释性和响应性。然而，中介效果高度依赖其LLM大脑，凸显了开发强中介层的必要性和社区协同的重要性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16610v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1426, \"height\": 761, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.16610v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1273, \"height\": 476, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16610v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1434, \"height\": 273, \"label\": \"Table\"}]"
motivation: 弥补长视界AI智能体工作流中缺失的持续可及语音中介层，增强用户对智能体执行的实时感知与干预能力。
method: 提出JarvisBench双赛道基准（协作与交互），构建参考语音中介原型，在34个文本任务上评估多种LLM智能体的中介效果。
result: 中介可基于执行轨迹回答用户，注入稀疏指导提升任务完成率，但效果高度依赖中介LLM的能力。
conclusion: 语音中介层能改善长工作流任务完成与用户交互，性能与底层LLM强相关，呼唤社区共同研发强中介智能体。
---

## 摘要
长周期人工智能智能体的能力正日益增强，但它们与用户的交互依然出奇地薄弱。在大多数工作流程中，用户给出初始指令后，只能收到选择性的文本更新，难以清楚了解智能体在做什么或何时介入。这导致当前智能体生态系统中缺失了一个环节：一个始终在线的、贾维斯式的中介，能让智能体与用户保持持续联系。这种中介应当支持与用户的实时语音交互，在不妨碍工作智能体的情况下回答问题，主动报告进展或困惑，并在有用时将用户引导注入智能体的执行过程。本文中，我们提出了 JarvisBench，一个用于衡量长周期智能体工作流中中介双重价值的基准。JarvisBench 包含两个互补的赛道：智能体协作赛道衡量中介是否改善下游任务完成情况，用户交互赛道衡量中介是否让执行过程更易理解、响应更及时、对用户更易访问。我们用一个模块化的参考 Jarvis 原型实例化该基准，并在 OpenClaw 中执行的 34 个纯文本 WildClaw 任务上进行评估。使用 GPT-5.5、Claude Opus 4.7、基于 Gemini 和基于 GPT 的工作智能体的初步结果表明，贾维斯式中介能够对用户问题给出基于轨迹的可靠回答，并在适当时候注入稀疏用户引导时提升任务性能。结果还表明，效果强烈依赖于中介的大语言模型大脑，这凸显了这一缺失中间层的前景，以及更广泛社区共同努力的必要性。演示页面：https://cchen1436.github.io/jarvis

## Abstract
Long-horizon AI agents are becoming increasingly capable, yet their interaction with users remains surprisingly thin. In most workflows, users give an initial instruction, receive only selective textual updates, and lose a clear sense of what the agent is doing or when to step in. This leaves a missing part in the current agent ecosystem: an always-on Jarvis-style mediator that keeps the agent continuously reachable to the user. Such a mediator should support real-time spoken interaction with the user, answer questions without interrupting the worker, proactively report progress or confusion, and inject user guidance back into the agent's execution when useful. In this work, we introduce JarvisBench, a benchmark for measuring the dual value of mediation in long-horizon agent workflows. JarvisBench contains two complementary tracks: an agent-collaboration track that measures whether mediation improves downstream task completion, and a user-interaction track that measures whether mediation makes ongoing execution more understandable, responsive, and accessible to users. We instantiate the benchmark with a modular reference Jarvis prototype and evaluate it on 34 text-only WildClaw tasks executed in OpenClaw. Preliminary results with GPT-5.5, Claude Opus 4.7, Gemini-based, and GPT-based worker agents suggest that Jarvis-style mediation can provide trace-grounded responses to user questions and improve task performance when sparse user guidance is injected at appropriate moments. The results also show that effectiveness depends strongly on the mediator's LLM brain, highlighting both the promise of this missing middle layer and the need for broader community effort. Demo page https://cchen1436.github.io/jarvis

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
*   **核心问题**：现有长周期（long‑horizon）AI 智能体虽然能力日益强大，但其用户交互十分薄弱，用户仅在初始时发出指令，随后只能被动等待文本更新，缺乏对智能体实时状态的理解和及时干预的机会。这导致用户与智能体之间存在一个“缺失的中间层”——一种始终在线、支持实时语音交互、能主动报告进展或困惑、并适时将用户引导返回执行流的中介。
*   **整体含义**：受科幻作品中贾维斯（J.A.R.V.I.S.）启发，论文主张在用户与执行具体任务的工作智能体之间引入一个**语音中介**，使智能体“持续可及”，从而提升任务成功率与用户体验。为此，提出 **JarvisBench** 基准，从**智能体协作**和**用户交互**两个维度量化这种中介的价值，并推动社区在该中间层上的研究。

### 2. 论文提出的方法论
*   **核心思想**：将中介设计为一个**可插拔的基准**，而非固定架构。中介观察工作智能体的执行轨迹（ReAct 风格），通过语音与用户沟通，并在必要时将用户反馈注入工作智能体的上下文。
*   **关键技术细节**：
    *   **双赛道基准设计**：
        *   **智能体协作赛道**：衡量中介能否改善下游任务完成度。中介监测工作智能体轨迹，识别故障敏感检查点（如重复工具失败、停滞、高风险操作），对“专家用户”发出询问，将简洁引导注入工作智能体。
        *   **用户交互赛道**：衡量中介能否让执行过程对“非专家用户”更易懂、更可及。用户只提进度型或话题型问题，不提供求解指导；中介基于可见轨迹回答。
    *   **参考原型（Jarvis）模块化实现**：
        *   **级联全双工语音接口**：流式 ASR + 语义 VAD（SoulX‑Duplug）判断用户是否说完、是否打断，文本由中介脑处理后通过 TTS（Chatterbox）回话，实现始终监听状态。
        *   **轨迹监控与上下文管理**：分离对话上下文（用户‑中介历史）与执行上下文（工作智能体的动作、观察、错误等），并维持控制状态记录当前中介角色（回答、等待专家指导、准备注入）。
        *   **交互检查点**：通过协作暂停机制实现可复现的实时交互，在用户交互赛道定时触发提问，在协作赛道稀疏触发于故障敏感点。
    *   **可插拔评估协议**：允许独立替换中介 LLM 脑、语音堆栈、监控策略、记忆设计或工作智能体，只要输出标准日志即可参与对比。
*   **公式或算法流程**：无明显公式，流程表现为：
    1.  工作智能体执行任务，产出流式文本轨迹。
    2.  中介接收轨迹，若在协作赛道且触发干预检查点，则暂停工作智能体，向专家用户询问并等待反馈，将反馈以指令形式注入工作智能体；在互赛道则在预设检查点向非专家用户回答进度/话题问题。
    3.  评估：任务完成度用 WildClaw 原生评分；交互质量用独立 LLM 评判（1‑5 分），分别考核回答是否基于轨迹（Progress）和是否与任务上下文一致（Topic），同时记录语音延迟。

### 3. 实验设计
*   **数据集与场景**：使用 **WildClawBench** 的 **34 个纯文本任务**，涵盖生产力流、代码智能、社交互动、搜索检索、创意综合和安全对齐等类别，要求多步规划、工具使用、错误恢复等。
*   **基准对比**：
    *   **智能体协作赛道**：同一工作智能体在“仅用 OpenClaw 基线”与“同一工作智能体 + Jarvis 中介”两种设置下对比。工作智能体包括 GPT‑5.4‑mini、GPT‑5.5、Gemini‑3.1‑Pro、Claude Opus 4.7。
    *   **用户交互赛道**：固定工作智能体为 GPT‑5.4‑mini，更换中介 LLM 脑进行对比：Qwen3‑30B‑A3B‑Instruct、Qwen3‑32B‑Thinking、GPT‑OSS‑120B、GPT‑5.4‑mini、GPT‑5.4。
*   **用户模拟器**：采用 GPT 支持的模拟器，非专家用户只提问不指导，专家用户仅在中介触发干预时给出简明纠正，以确保中介是“沟通桥梁”而非第二求解器。
*   **评判协议**：任务完成沿用 WildClaw 的评判；交互回答则用 LLM 评审（独立于被评测模型）并分别评估进度和话题。

### 4. 资源与算力
*   论文未明确提及所使用的具体 GPU 型号、数量或训练/微调耗时。文中仅指出使用 GPT‑5.4 等闭源模型作为中介脑和用户模拟器，语音模块采用现成工具（SoulX‑Duplug、Chatterbox），实验集中于推理而非大规模训练，因此**算力开销未被详细报告**。

### 5. 实验数量与充分性
*   **实验组数**：
    *   智能体协作赛道：4 个不同工作智能体 × 2 种配置（有无中介），共 **8 组对比**，报告总体得分、提升、满分任务数、干预次数。
    *   用户交互赛道：1 个工作智能体 × 5 种中介脑，共 **5 组对比**，报告延迟、进度和话题得分及高分占比。
    *   此外提供少量定性案例（3 个协作案例、若干交互案例）以及不同模型的典型失败模式分析。
*   **充分性与公平性**：
    *   实验覆盖了多个当前主流的大语言模型（闭源与开源），并采用统一的评判协议，具有较好的可复现性和公平性。
    *   不足之处：任务仅限 34 个纯文本，未涉及多模态任务；未对中介的干预策略、检查点选择频率等做消融实验；用户模拟器全部基于 GPT，可能与真实人类交互行为存在偏差；缺乏与其他可能中介架构的直接比较（因为 JarvisBench 是新提出的基准）。

### 6. 论文的主要结论与发现
*   在 **智能体协作赛道** 上，Jarvis 中介使所有测试的工作智能体的 WildClaw 任务总得分**均有提升**（+4.86% 至 +11.78%），其中 Gemini‑3.1‑Pro 和 Claude Opus 4.7 提升最显著。
*   提升主要来源于**识别高杠杆故障点并注入简洁纠正性引导**，而非替代工作智能体；引导即便简单，也能帮助智能体摆脱局部失败。
*   在 **用户交互赛道** 上，**中介脑的能力强烈影响回答质量**：GPT‑5.4 话题性最佳，GPT‑5.4‑mini、GPT‑OSS‑120B 在进度解释上表现相近，而 Qwen 系列在话题一致性上较差，且易受实时更新上下文干扰。
*   中介效果**高度依赖 LLM 脑**：弱模型可能引入不当干预或低质量回答，强模型则更稳定。
*   中介层不仅能充当可用性细节，更是让强大智能体透明、可操控、与用户对齐的**实用组件**。

### 7. 优点
*   **问题定义新颖**：明确提出长周期智能体缺失的中间层问题，并将其形式化为可量化的基准。
*   **双维度评估**：同时考量代理性能和用户体验，更全面地反映中介的价值。
*   **模块化与可插拔设计**：分离各组件，方便后续研究者替换并对比特定模块，具有良好的生态扩展性。
*   **实验广度**：在多种当前领先的大模型上验证了中介的作用，并分析了不同模型的失败模式。
*   **参考原型完整**：从语音接口到上下文管理、检查点机制，提供了实用的实现蓝图，并辅以定性案例说明。

### 8. 不足与局限
*   **任务范围受限**：仅使用 34 个纯文本任务，未涵盖原 WildClaw 中的图像、视频等多模态任务，结论的泛化性有待验证。
*   **用户模拟偏差**：用户模拟器基于 GPT 模型，可能过于理性或缺乏真实用户的多样性与非理想交互，可能高估或低估实际使用效果。
*   **干预策略粗放**：未对不同检查点触发策略、干预频率、引导注入方式等做详细消融研究，无法判断当前配置是否为最优。
*   **算力信息缺失**：未报告任何资源开销，不利于复现和成本评估。
*   **强模型依赖**：结论显示效果与中介脑能力强绑定，对开源或较小模型的支持可能不够稳健，且当前评估依赖闭源大模型。
*   **基准竞争有限**：作为新提出的基准，尚没有其他中介系统在同一设定下进行直接横向比较，仅展示了自身原型的效果。

（完）
