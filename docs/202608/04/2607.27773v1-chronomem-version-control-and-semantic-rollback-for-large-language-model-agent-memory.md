---
title: "ChronoMem: Version Control and Semantic Rollback for Large Language Model Agent Memory"
title_zh: ChronoMem：面向大型语言模型智能体记忆的版本控制与语义回滚
authors: "Yongye Su, Wujiang Xu, Chaoji Zuo, Elisa Bertino"
date: 2026-07-30
pdf: "https://arxiv.org/pdf/2607.27773v1"
tags: ["query:agent-memory"]
score: 10.0
evidence: 提出智能体记忆的语义版本控制层
tldr: 现有LLM智能体记忆系统仅支持前向积累，缺乏版本控制与回滚机制，导致纠正错误、概念漂移时脆弱。ChronoMem引入语义版本控制层，通过全内存快照、混合检索与重排序实现自然语言驱动的记忆回滚。在长对话基准上，该方法显著提升回滚一致性问答与摘要性能，语义版本选择准确。这是首个开源系统，为智能体记忆提供系统化语义回滚与评估基准，增强可靠性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.27773v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1826, \"height\": 1111, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.27773v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1823, \"height\": 834, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.27773v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1817, \"height\": 294, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.27773v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 870, \"height\": 374, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.27773v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 784, \"height\": 235, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.27773v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 839, \"height\": 254, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.27773v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 807, \"height\": 235, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.27773v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 787, \"height\": 236, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.27773v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 787, \"height\": 235, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.27773v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 801, \"height\": 235, \"label\": \"Table\"}]"
motivation: 智能体记忆系统缺少历史状态检查与回滚能力，易受记忆损坏和后续信息干扰。
method: 每次写入提交全内存快照，以混合词汇语义检索、排序融合和重排序实现自然语言版本回滚。
result: 回滚一致性问答和摘要性能大幅超越基线，语义版本选择准确率强劲。
conclusion: ChronoMem是首个开源智能体记忆语义回滚系统，提供版本控制与基准，提升可靠性。
---

## 摘要
大型语言模型智能体越来越依赖长期记忆来支持多会话交互和个性化。然而，现有的智能体记忆系统仅围绕前向演化设计，持续累积、巩固和覆盖知识，缺乏检验、版本控制或回退先前状态的原则性机制。这使得智能体在面对纠正、概念漂移和记忆损坏时变得脆弱，尤其在已经接触到后续信息之后。我们提出了 ChronoMem，一个面向智能体记忆的语义版本控制层，已集成到谷歌生产就绪的开源 Agent Development Kit 中。ChronoMem 在每次记忆写入时提交全量记忆快照，维护结构化的版本历史，并通过混合词汇与语义检索、排序融合和重排序，将撤销意图映射到具体的历史版本，从而支持自然语言回滚请求。我们进一步引入了暴露后评估协议，该协议测试智能体在回滚后能否以反事实方式回答问题并总结历史，就像未来的更新从未发生过一样。在增加了演化记忆状态和回滚任务的长程对话基准上，相比仅提示和仅检索的基线，ChronoMem 显著提升了回滚一致性问答和历史摘要的性能，同时在语义版本选择上表现出色。据我们所知，ChronoMem 是首个用于大型语言模型智能体中系统性语义全局记忆回滚的开源系统和基准。

## Abstract
LLM agents increasingly rely on long-term memory to support multi-session interaction and personalization. However, existing agent memory systems are designed around forward-only evolution, continuously accumulating, consolidating, and overwriting knowledge, with no principled mechanism to inspect, version, or revert prior states. This makes agents brittle under corrections, concept drift, and memory corruption, particularly after they have already been exposed to subsequent information. We present ChronoMem, a semantic version-control layer for agentic memory integrated into the production-ready, open-source Agent Development Kit by Google. ChronoMem commits whole-memory snapshots at each memory write, maintains structured version histories, and supports natural-language rollback requests by mapping undo intents to concrete historical versions through hybrid lexical and semantic retrieval, rank fusion, and reranking. We further introduce a post-exposure evaluation protocol that tests whether an agent can behave counterfactually after rollback by answering queries and summarizing history as if future updates had never occurred. On long-horizon conversational benchmarks augmented with evolving memory states and rollback tasks, ChronoMem substantially improves rollback-consistent question answering and history summarization relative to prompt-only and retrieval-only baselines, while achieving strong performance in semantic version selection. To our knowledge, ChronoMem is the first open-source system and benchmark for systematic semantic global memory rollback in LLM agents.

---

## 论文详细总结（自动生成）

好的，作为资深学术论文分析助手，我将基于您提供的论文内容，生成一份结构化的中文总结。

### 1. 核心问题与研究动机

-   **核心问题**：现有大型语言模型智能体的长期记忆系统是一种“仅前向”演化的模型，知识被持续累积、整合和覆盖。这种设计缺少对记忆历史状态的检查、版本控制和回滚机制。
-   **研究动机与背景**：
    -   **脆弱性**：当智能体记忆发生错误、概念漂移或被恶意污染时，尤其是已经处理了后续信息后，上述设计使智能体变得非常脆弱，难以修正。
    -   **现有机制的局限**：
        -   开源框架中的会话重绕功能是会话级别的，不恢复全局记忆状态。
        -   云服务的记忆修订功能是基于不透明版本ID的，不支持基于自然语言的语义回滚。
        -   现有的长程记忆基准测试均假设记忆是前向演化的，不评估回滚到先前全局状态的能力。
    -   **研究空白**：业界缺少一个开源的、能够根据自然语言“撤销”指令，系统性地将智能体全局记忆回滚到先前一致状态，并在暴露后评估其行为一致性的系统与基准。

### 2. 方法论

-   **核心思想**：将智能体的长期记忆视为一个**版本化状态机**。每次记忆写入操作自动创建一个包含全局状态的“提交”快照。回滚操作则恢复到一个历史快照，并确保后续所有记忆读写都限定在该版本范围内，从而实现反事实行为。
-   **关键技术细节与架构**：
    -   **系统架构**：ChronoMem 作为一个版本化的 `MemoryService` 包装器，内含编排器、SQLite 存储和重排序模块，集成于谷歌的 ADK 框架。
    -   **状态模型**：采用“事件溯源”思想，维护一个仅追加的不可变事件日志，但通过直接存储完整的“内存快照”来避免回滚时重放整个日志，以存储空间换取快速恢复。
    -   **四阶段处理流程**：
        1.  **版本构建（提交即写）**：每次记忆写入触发一个事务，生成不可变事件、物化全局快照，并将活跃版本指针指向新版本。
        2.  **版本索引（语义提交描述符）**：为每个版本创建一个描述符，包含操作变化、自然语言摘要、操作类型和时间标签等。这构建了用于语义检索的二级索引，其规模与提交次数而非快照大小相关。
        3.  **自然语言到版本的映射**：接收自然语言回滚请求，通过**混合检索**流程定位目标版本。
            -   **词法检索**：在提交描述符上使用 BM25 进行关键词匹配。
            -   **语义检索**：对提交描述符进行向量嵌入，通过近似最近邻搜索。
            -   **排序融合**：使用倒数排序融合算法合并词法和语义检索的结果。
            -   **重排序**：使用一个更强的跨编码器模型对融合后的候选版本进行最终排序，选出最佳目标版本。
        4.  **版本回滚执行**：根据解析出的版本ID，通过确定性的数据面原语执行操作：加载快照、恢复状态、更新活跃指针、截断后续版本，并尽力同步外部检索引擎，从而完成全局状态的回滚。

### 3. 实验设计

-   **数据集与场景**：
    -   **LoCoMo**：用于评估长程对话场景下的回滚一致性问答和历史摘要。
    -   **MemoryAgentBench**：用于评测在频繁更新的增量式记忆场景下的回滚能力。
    -   **暴露后回滚协议**：设计了一个两阶段评估协议。首先，让智能体处理完所有会话，达到最终记忆状态；然后，发出一个自然语言回滚指令，将记忆恢复到某个中间历史版本；最后，在恢复后的状态下执行问答或摘要任务，测试其行为是否具有反事实性。
-   **基准对比方法**：
    -   **仅提示回滚**：不改变记忆状态，仅通过指令让模型忽略后续信息。
    -   **全历史提示回滚**：在提示词中包含完整交互历史，但要求模型表现得像已回滚一样。
    -   **仅检索回滚**：不恢复快照，仅通过向量相似度检索与查询相关的历史记忆片段来生成答案。
-   **评估指标**：
    -   **语义版本选择**：`Recall@k` (目标版本是否在Top-k候选中) 和 `Scope@k` (所选版本与目标版本的时间邻近度)。
    -   **回滚一致性问答**：`F1` 分数和 `Exact Match`。
    -   **回滚一致性摘要**：`ROUGE-1` 分数。

### 4. 资源与算力

-   **未明确提及**：论文并未提及训练或实验中所使用的具体 GPU 型号、数量或训练时长。文中仅说明评估了三种不同规模的基座模型，并使用 Cohere Rerank-3.5 作为跨编码器重排序模型，但未报告运行这些模型所需的具体算力。

### 5. 实验数量与充分性

-   **实验数量**：实验覆盖了两大数据集、三个核心研究问题，并对比了 ChronoMem 与三组基线方法在三种不同基座模型下的表现。总计约进行了数十组主要实验。
-   **充分性与公平性**：
    -   **充分性**：实验设计比较充分。它系统地解耦了“语义版本选择”和“回滚后任务执行”两个环节，避免了选错版本对下游任务评估的干扰。多个数据集和多种基座模型的运用增强了结论的可靠性。
    -   **客观公平性**：评估比较客观。所有下游任务指标都报告在同一组回滚实例上，确保了性能提升与版本选择质量解耦。对比的基线方法涵盖了从简单提示到高级检索增强的不同层级，力图证明 ChronoMem 架构的独特价值。

### 6. 主要结论与发现

-   **语义版本选择**：ChronoMem 的混合检索加重排序方法显著优于单独的 BM25、稠密检索或融合方法，表明时间感知的重排序对消歧至关重要。
-   **回滚一致性问答**：在暴露后回滚设定下，ChronoMem 的问答性能远超所有基线。仅靠提示指令无法有效抑制后版本信息，证明显式的版本化状态隔离是必需的。
-   **回滚一致性摘要**：ChronoMem 在生成与历史状态一致的摘要方面同样表现最佳。基线方法要么难以忽略后续信息，要么因依赖碎片化的检索结果而无法把握全局。
-   **核心洞见**：回滚一致性行为**主要是一种架构特性**，它依赖于显式的、版本限定状态恢复，而不能可靠地从指令跟随或片段检索中涌现出来。

### 7. 优点

-   **问题新颖**：首次系统性地定义并解决了智能体记忆的语义全局回滚问题，填补了从“仅前向”积累到“可逆管理”的空白。
-   **方案系统性**：设计了一个完整、清晰的系统，包含版本构建、索引、语义解析和确定性的回滚执行，并提出了暴露后评估协议，为后续研究奠定了基础。
-   **工程实践性强**：该系统基于开源且生产就绪的 ADK 框架实现，采用 SQLite 等轻量级组件，易于集成和推广。
-   **评估严谨**：实验设计精细，通过解耦版本选择和任务执行，使评估结果更加可靠，结论更具说服力。

### 8. 不足与局限

-   **并发限制**：系统使用单用户锁和 SQLite 文件级锁，未在高并发场景下进行实验评估，实际应用可能面临写入瓶颈。
-   **版本线性历史**：仅支持线性版本历史，回滚操作会直接截断后续版本，不支持分支和合并，功能不够灵活。
-   **基准测试偏差**：用于评估的 LoCoMo 和 MAB 基准并非为版本化回滚场景原生设计，其构造的回滚查询可能无法完全代表真实用户意图的多样性和模糊性。
-   **算法细节缺失**：论文未公开具体的 prompt 设计，可能导致结果无法复现；未披露所用基座模型版本，影响结果复现的确定性。
-   **外部副作用**：系统明确定义其回滚范围仅限于智能体记忆内部，不涉及撤销外部工具调用的副作用，这在实际部署中仍是一个未解决的问题。

（完）
