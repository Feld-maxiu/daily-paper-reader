---
title: "Teach and Grow: An Agent-Centered Architecture for General Robot Learning"
title_zh: 教导与成长：面向通用机器人学习的以智能体为中心的架构
authors: "Chang Nie, Zhe Liu, Hesheng Wang"
date: 2026-08-17
pdf: "https://arxiv.org/pdf/2608.17209v1"
tags: ["query:agent-memory"]
score: 8.0
evidence: 多模态智能体将演示转化为可复用技能块
tldr: 端到端VLA模型受已验证物理覆盖限制，遇到新对象或接触需重新采集数据与策略更新，形成重训练税。TGL智能体将少量演示转化为可复用技能块，在新场景中组合并基于物理结果修订路线。在LIBERO上取得最先进性能，实验证实技能归纳、持久重用与自适应。方法无需任务特定策略重训练，并提出部署即持续学习的缩放律假设，使一任务促进下一任务。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.17209v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 912, \"height\": 695, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.17209v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1743, \"height\": 862, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.17209v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1782, \"height\": 845, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.17209v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1851, \"height\": 932, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.17209v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1828, \"height\": 903, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.17209v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1526, \"height\": 1193, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.17209v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1806, \"height\": 345, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.17209v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1835, \"height\": 588, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.17209v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1745, \"height\": 494, \"label\": \"Table\"}]"
motivation: 端到端VLA模型受已验证物理覆盖限制，遇到新对象或接触需重新采集数据、更新策略和回归测试，形成重训练税。
method: 智能体将少量演示转化为可复用技能块，在新场景中组合并基于物理结果修订路线，无需任务特定策略重训练。
result: 在LIBERO基准上取得最先进性能，控制实验验证技能归纳、持久重用与智能体引导自适应。
conclusion: 提出TGL将部署视为持续学习，并提出以有效可重用经验X的幂律逼近不可约误差下限的缩放律假设。
---

## 摘要
端到端视觉-语言-动作（VLA）和世界-动作模型为通用机器人提供了一条优雅的路径，但其可靠性受限于已验证的物理覆盖范围。当不熟悉的对象、传感器、机器人形态或接触超出该覆盖范围且没有经过验证的备选方案时，纠正失误需要新的机器人数据、策略更新和回归测试。这种反复出现的负担就是再训练税。与文本不同，具身数据通常必须通过操作机器来生成。我们提出了教导与成长学习（TGL），一种面向通用机器人学习的以智能体为中心的架构。在其一般形式中，多模态智能体将少量成功的演示转化为可重用的技能块：针对有意义子目标的闭环行为。在新场景中，智能体将这些技能块进行接地和组合，选择学习到的或几何工具，观察物理结果，并在执行偏离意图时修正路径。技能库存储可执行行为，而结构化的经验记忆将成功、失败和修复向前传递。新任务无需针对特定任务进行策略再训练即可获得。我们的LIBERO评估获得了最先进的性能；受控研究揭示了技能归纳、持续重用和智能体指导的适应。最后，我们提出教导与成长缩放定律假设：如果X表示有效的可重用经验，那么未来任务误差和教学需求应以X的幂律形式趋近于不可约下限。因此，该架构将部署视为一个持续学习的时期，在这个时期中，一个任务可以使下一个任务更容易。

## Abstract
End-to-end vision-language-action (VLA) and world-action models offer an elegant route to general-purpose robotics, but their reliability is bounded by validated physical coverage. When an unfamiliar object, sensor, embodiment, or contact falls outside that coverage and no validated fallback exists, correcting the failure requires new robot data, a policy update, and regression testing. This recurring burden is the retraining tax. Unlike text, embodied data must often be created by operating machines. We present Teach-and-Grow Learning (TGL), an agent-centered architecture for general robot learning. In its general form, a multimodal agent turns a few successful demonstrations into reusable Skill Blocks: closed-loop behaviors for meaningful subgoals. In a new scene, the agent grounds and composes these blocks, selects learned or geometric tools, observes the physical outcome, and revises the route when execution departs from intent. A Skill Library stores executable behavior, while structured Experience Memory carries forward success, failure, and repair. New tasks are acquired without task-specific policy retraining. Our LIBERO evaluation attains state-of-the-art performance; controlled studies expose skill induction, persistent reuse, and agent-directed adaptation. Finally, we propose the Teach-and-Grow scaling-law hypothesis: if X denotes effective reusable experience, future-task error and teaching demand should approach irreducible floors as power laws in X. The architecture therefore treats deployment as a period of continued learning, in which one task can make the next easier.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **研究背景与动机**  
  端到端视觉-语言-动作（VLA）与世界-动作模型（WAM）虽然提供了通用机器人的简洁路线，但其可靠性受限于**已验证的物理覆盖范围**。一旦遇到不熟悉的对象、传感器、机器人形态或接触条件，且没有经过验证的备选方案，纠正失误就需要重新采集机器人数据、更新策略并进行回归测试。  
  论文将这种反复出现的负担称为**“再训练税”（retraining tax）**：本地缺口引发全局更新，导致覆盖面扩大时成本急剧上升。

- **整体含义**  
  TGL 将部署视为**持续学习期**：机器人从少量成功演示中获取任务结构，形成可复用技能块，在新场景中组合、执行、观察物理结果并修订路线，从而在**不重训任务特定策略**的情况下获得新能力。其核心思想类似“教学相长”——教师的演示提供初始结构，机器人的执行反馈反过来暴露知识缺口，使后续教学更加精准。

## 2. 论文提出的方法论

- **核心思想**  
  TGL 将机器人学习从“改变模型权重”转向“改变可检索、可执行的显式知识与记忆”。  
  预训练基础模型参数 θ 保持固定，系统通过更新 **技能库 B** 和 **经验记忆 M** 来学习：

  ```
  θ_{n+1} = θ_n
  (B_{n+1}, M_{n+1}) = U(B_n, M_n, D_n, ΔH_n)
  H_{n+1} = H_n ∪ ΔH_n
  ```

- **技能块（Skill Blocks）**  
  每个技能块是一个可复用的闭环行为，定义为：

  ```
  b_i := ⟨g_i, S_i, ρ_i, γ_i, Π_i, v_i, R_i⟩
  ```

  各字段含义：目标 `g_i`、适用条件 `S_i`、可复用策略 `ρ_i`、观测到接地变量的映射 `γ_i`、兼容执行器集合 `Π_i`、成功判定 `v_i`（返回 pass/fail/inconclusive）、恢复选项 `R_i`。  
  技能块不是轨迹片段回放，而是在当前场景中**重新实例化**对象、几何、抓取、路径与控制目标。

- **动态组合与物理反馈**  
  智能体在语义决策层构建有序工作计划 `τ_t = (b_{i_{t,1}}, ..., b_{i_{t,L_t}})`，但该计划是可变的。每执行一个块后，根据物理结果决定继续、调整、替换剩余路径或请求教学：

  ```
  (o_{t+1}, ε_t) = Run(b_{i_{t,1}}, π_{i_{t,1},t}, o_t)
  ```

  其中 `ε_t` 记录执行器与验证器证据，用于判断预期效果是否达成。

- **结构化经验记忆**  
  每条经验记录为：

  ```
  μ_j = ⟨task, context, blocks, outcome, diagnosis, repair, evidence⟩
  ```

  记录成功、失败原因与修复策略，为后续任务提供可检索的文本指导。

- **缩放律假设**  
  定义有效可重用经验 `X_n` 为历史交互的加权和（权重 ω ∈ [0,1]，考虑证据可靠性、覆盖增量、可检索性、接地有效性与兼容性）。  
  提出假设：

  ```
  E_future(X) = E_∞ + A X^{−α},   D_teach(X) = D_∞ + B X^{−β}
  ```

  即未来任务误差与所需教学量随 X 按幂律趋近不可约下限。

## 3. 实验设计

- **基准数据集**  
  主要使用 **LIBERO** 基准，包括 LIBERO-Object 和 LIBERO-GOAL 等场景。

- **实验类型**  
  1. **标准 LIBERO 评估**：声称取得最先进（SOTA）任务成功率，但正文未给出具体数值。  
  2. **受控研究**（见表 I）：
     - **从演示到持久块**：3 条教师轨迹生成两个技能块（获取对象、按关系释放），在 3 个分离状态上均成功，保存/重载后仍成功，在超出所学范围的状态上正确停止。  
     - **反馈改变工作流**：碗-盘任务中拾取成功后证据不足，智能体重建剩余路线并完成；抽屉打开任务中不确定结果触发重新观察。  
     - **固定执行器库增长**：6 块库在 6 个评估状态上 0/6，增加 2 个学习块后 4/6；对应 95% 置信区间分别为 [0.00, 0.39] 和 [0.30, 0.90]，Fisher 精确检验 p=0.061。

- **对比方法**  
  论文未进行严格定量对比实验，主要与 **端到端 VLA/WAM** 在架构层面对比（图 1），并在表 II 中将 TGL 与行为克隆、强化学习在“学习对象”“更新机制”“演示角色”“持久性”等方面定性对比。  
  附录中提出未来纵向研究将以原始情景记忆、固定工具管道、顺序 VLA 适应、Agent–VLA 混合体作为匹配对照。

## 4. 资源与算力

- **未明确说明**。  
  论文正文与附录均未提及 GPU 型号、数量、训练时长或具体算力配置。  
  附录 I 提到 Agent 配置使用 `gpt-5.6-sol`（seed 0），但这属于模型接口，不是算力资源说明。  
  整体实验规模较小（少量演示、个别任务、单种子），对算力需求不高，但缺少具体资源记录。

## 5. 实验数量与充分性

- **实验数量有限**  
  核心受控实验包含：
  - 视觉演示分解：10 条演示，40 个预测阶段，边界 F1 在精确匹配为 0.100，±2 帧内为 0.900；20/20 效果确认。
  - 任务特定技能学习：3 条教师轨迹，分离状态 3/3 成功，远状态正确停止。
  - 固定执行器试点：6 块 vs 8 块，各 6 个评估，0/6 vs 4/6，单种子。
  - 失败定位队列：8 次尝试全部失败，定位到 2 个规划前、4 个路径一致性/校准、2 个夹爪关闭问题。

- **充分性与客观性评价**  
  - **优点**：实验设计围绕机制验证（技能归纳、持久性、反馈驱动、局部增长），且附录给出生数据与统计区间。
  - **不足**：样本量极小，单种子，置信区间宽（0/6 vs 4/6 未达到统计显著，p=0.061）；标准 LIBERO 声称 SOTA 但未提供具体数值或与其他方法的定量对比；缩放律假设仅处于提案阶段，尚无纵向实验验证。  
  因此实验**不足以严格证明**方法的普适性和统计可靠性，更适合作为概念验证与机制展示。

## 6. 论文的主要结论与发现

- TGL 能够从少量演示中归纳出**持久、可复用、范围感知**的技能块，这些块可保存、重载并在新场景中重新接地。
- 物理反馈确实会改变智能体的决策：重新规划或重新观察能够纠正执行偏差。
- 在固定执行器条件下，**局部增加技能块**就能改变同一执行器的行为，无需重训整个策略（0/6 → 4/6）。
- 提出**教导与成长缩放律假设**：有效可复用经验 X 的增长会以幂律形式降低未来任务误差和教学需求，使部署成为持续学习过程。

## 7. 优点

- **架构分离清晰**：固定基础模型 θ、可执行技能库 B、经验记忆 M、交互历史 H 各司其职，避免全局重训。
- **显式、可编辑的知识**：技能块与经验记忆均为结构化文本，人类可检查、修订，提升了可解释性与安全性。
- **本地更新机制**：新增技能或修复只影响相关块，降低回归测试成本，避免“再训练税”。
- **闭环验证与反馈**：每个块执行后必须验证预期效果，防止错误沿任务链传播；智能体在语义边界而非高频运动层做决策。
- **教学与探索结合**：以少量演示为起点，用物理反馈填补缺口，既避免了盲目探索的不安全，又超越教师演示范围。
- **可测试的缩放律**：将“有效经验”而非“存储数量”作为缩放资源，并给出具体测量方法（ω 加权、纵向实验设计）。

## 8. 不足与局限

- **时间成本高**：陌生任务需要多轮智能体推理、工具调用与观测，目前比前馈策略推理慢，需依赖未来模型加速与蒸馏。
- **实验规模与统计性**：受控实验样本量小、单种子、部分结果未达显著（p=0.061），难以支撑强结论。
- **缺乏定量基准对比**：标准 LIBERO 声称 SOTA，但正文未提供具体成功率数值，也未与其他方法进行系统比较。
- **缩放律尚未验证**：仅提出假设，尚无纵向实验数据支持幂律关系。
- **环境限制**：仅在 LIBERO 仿真环境中验证，未涉及真实机器人、力控、触觉、多传感器或跨机器人形态。
- **通用性风险**：技能块抽象、分解与对齐过程依赖当前多模态 Agent 能力，可能在不同任务分布下表现不稳定；附录中的视觉分解边界 F1 在精确匹配下仅 0.100，说明时序对齐仍有较大误差。

（完）
