---
title: "Continuous Improvement and Parallel Autonomous Exploration: An LLM-Agent Framework for Searching Large Solution Spaces"
title_zh: 持续改进与并行自主探索：一个用于搜索大型解空间的 LLM-Agent 框架
authors: "Dulmini Hettiarachchi, Andre Rusli, Julio Christian Young, Sho Akiyama"
date: 2026-08-05
pdf: "https://arxiv.org/pdf/2608.04341v1"
tags: ["query:agent-memory"]
score: 8.0
evidence: 利用排行榜奖励驱动智能体持续改进与并行探索的框架
tldr: "针对大解空间搜索效率低、依赖人工设计的挑战，本文提出LLM-Agent框架，通过排行榜奖励驱动单智能体持续改进，并支持多智能体全自主并行探索。在电商产品目录匹配任务中，单智能体在精确率≥95%下实现47.8%-57.4%的条件覆盖率，五智能体并行则提升至62.8%-69.4%，远高于33.3%的基线。该框架为大规模解空间的自主搜索与覆盖扩展提供了有效且可扩展的范式。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.04341v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 874, \"height\": 666, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.04341v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 916, \"height\": 395, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.04341v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 660, \"height\": 254, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.04341v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 831, \"height\": 291, \"label\": \"Table\"}]"
motivation: 现有LLM代理在搜索大型解空间时缺乏有效自主探索机制，需人工设计范式且难以覆盖多样化解。
method: 利用留存数据构建排行榜作为奖励信号驱动单智能体迭代改进，并支持多个智能体全自动并行探索，通过主持人代理协调。
result: "在达到每类目P@1≥95%的条件下，单智能体覆盖率达47.8-57.4%，五智能体达62.8-69.4%，远超基线33.3%。"
conclusion: 该框架实现持续改进和并行自主探索，有效扩展解空间覆盖，为复杂检索任务提供可扩展的自主解决方案。
---

## 摘要
我们提出了一个框架，为 LLM 智能体提供了两种自主搜索大型解空间的机制。首先，基于留出数据评分的排行榜充当奖励信号，驱动每个智能体在反复提交中优化其解决方案，这一循环即使仅由单个智能体也能运行。其次，该框架支持完全自主地并行运行多个智能体，无需人类参与：智能体独立地进行分析、方法调研、实现、自我评估、提交和修订，而协调者智能体仅处理后勤事务。在共享奖励下并行运行智能体，能拓宽解空间的探索区域，而非仅限于改进单一的种子范式。我们在产品与目录匹配（一项核心电商检索任务，具有庞大的、按类别结构化的解空间）上实例化了该框架，并将其表示为一种在精确率与覆盖率之间权衡工作的选择性预测。单个智能体在其种子范式内进行优化，而并行自主智能体则浮现出质的不同解决方案。在该测试平台上，最佳合格覆盖率（每个类别 P@1 ≥ 95%）在单个智能体下达到 47.8-57.4%，五个智能体下达到 62.8-69.4%，而基线为 33.3%。我们的贡献在于框架本身：一个持续改进的奖励循环，以及一个支持完全自主并行探索的基础，并辅以案例研究证据。

## Abstract
We present a framework that gives LLM agents two mechanisms for searching large solution spaces autonomously. First, a leaderboard scored on held-out data acts as a reward signal that drives each agent to refine its solutions over repeated submissions, a loop that operates even with a single agent. Second, the framework enables running many agents in parallel, fully autonomously, with no human in the loop: agents independently analyze, survey methods, implement, self-evaluate, submit, and revise, while a moderator agent handles only logistics. Running agents in parallel under the shared reward broadens the explored region of the solution space rather than refining the single seeded paradigm. We instantiate the framework on product-to-catalog matching (a core e-commerce retrieval task with a large, category-structured solution space), posed as selective prediction with a precision-coverage operating point. A single agent refines within its seeded paradigm, whereas parallel autonomous agents surface qualitatively different solutions. On this testbed, best qualified coverage (>=95% P@1 per category) reaches 47.8-57.4% with a single agent and 62.8-69.4% with five, against a 33.3% baseline. Our contribution is the framework itself: a continuous-improvement reward loop and a substrate for fully autonomous parallel exploration, backed by case-study evidence.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

- **研究动机**：大型语言模型（LLM）驱动智能体在自动化代码编写、实验迭代等方面展现出潜力，但许多实际优化问题具有庞大的解空间，且往往需要针对特定任务的优化策略，传统方法依赖人工设计范式，探索效率有限。
- **核心问题**：如何构建一种框架，使 LLM 智能体能够在大型解空间中自主、高效地进行搜索，既能通过外部反馈实现持续改进，又能在没有人类干预的情况下拓宽搜索范围，避免陷入单一范式的局部最优。
- **整体含义**：论文提出一个通用框架，将留出数据上的排行榜作为奖励信号，实现智能体的迭代改进；同时支持多个智能体全自主并行探索，在不显式引入多样性保持算子时，仅靠共享奖励就引导搜索向差异化的方向扩展，从而更全面地覆盖解空间。

### 2. 论文提出的方法论

- **核心思想**：框架包含两个关键机制——**持续改进循环**和**并行自主探索**。每个智能体独立执行完整的研究循环（分析、调研、假设、实现、自评、提交、修订），提交结果由**基于留出测试集的排行榜**评分并反馈；多个智能体并行运行，共享排行榜信息（分数与方法摘要）但隔离代码与制品，通过竞争性的奖励驱动方法分化。
- **技术细节**：
  - **任务形式化**：将产品到目录匹配定义为**选择性预测**问题。给定用户商品列表 \(q\)，需要从对应类目下的目录库存单元集合 \(S_{c_q}\) 中检索正确 SKU \(s^*\)。每个预测附带置信度，低于阈值则放弃，从而在覆盖率与精确率之间权衡。评估指标为每个类目独立的 P@1（≥95% 才算合格）和平均覆盖率，最终目标是最大化合格条件下的平均覆盖率。
  - **基础设施**：
    - **自动化排行榜**：基于不可见的测试集真实标签计算 P@1 与覆盖率，判断是否合格（每类目 P@1≥95%），按平均覆盖率排序，将结果追加至持久化 JSON 账本，供所有智能体查看。
    - **训练/测试隔离与完整性**：训练数据含真实标签供自我评估；测试数据无标签，强制通过排行榜评分。通过严格的权限设置与自动化泄漏检查确保数据隔离，每个智能体仅限于自身工作目录和只读共享资源。
    - **GPU 资源管理**：多个智能体共享单个 GPU 节点，采用基于声明/释放的两级锁协议，并由系统级监视器终止未授权进程，防止并发使用造成的崩溃。
  - **自主运行**：人类仅在启动时指定智能体数量和总时间预算，协调者智能体仅处理时间提醒、排行榜更新、资源排队等后勤任务，不参与技术指导。每个智能体被设计为连续运行（周期性心跳、强制进度记录），在预算耗尽前持续提交与修订。
- **智能体设置**：每个智能体基于 Claude Sonnet 3.5，配备 Python 环境和嵌入模型（BGE‑M3、Qwen3‑Embedding）、FAISS 索引、TF‑IDF 库等工具。智能体接收任务简报（含基线方法），遵循“分析 → 调研 → 假设 → 实现 → 训练集自评 → 提交测试预测 → 观察排行榜 → 修订”的循环。

### 3. 实验设计

- **数据集/场景**：来自日本大型 C2C 电商平台的三个类目（智能手机、卡牌（其他）、卡牌（宝可梦）），训练集包含真实标签供自我评估，测试集标签由排行榜服务器严格保密。目录规模约 33K SKU。
- **基线方法**：使用 BGE‑M3 嵌入 + 余弦相似度，并在训练集上为每个类目独立调整置信度阈值以达到 95% P@1，然后应用于测试集。基线整体平均覆盖率为 33.3%，但不合格（卡牌（其他）类目 P@1 低于 95%）。
- **对比配置**：
  - **单智能体**：1 个智能体独立迭代，运行 3 次。
  - **并行（5 智能体）**：5 个智能体同时运行，完全自主，共享 GPU 和排行榜，运行 3 次。
  - 所有配置在相同基础设施、相同 5 小时时间预算下进行，且提交次数无上限。

### 4. 资源与算力

- 文中明确指出智能体共享**一个 GPU 节点**，但未列出具体 GPU 型号。由于早期直接并发使用导致崩溃，特别设计了 GPU 锁协议。
- 每个实验会话时间预算为 **5 小时**，每种配置（单智能体、5 智能体）各重复 **3 次**，因此总计 6 次完整会话，总 GPU 时间约 30 小时（不包含测试标签计算开销）。
- 除 GPU 外，涉及的是 CPU 上的轻量字符串处理、嵌入模型推理和 FAISS 索引，论文没有提供更多算力细节，但框架的设计明显面向普通开发环境。

### 5. 实验数量与充分性

- **实验组数**：两个主要配置（单智能体 vs. 5 智能体），每个配置独立运行 3 次（共 6 次独立实验）。实验还报告了每次运行的提交次数、合格提交次数等详细运行记录。
- **充分性分析**：
  - 优点：
    - 每个配置多次重复，增强了结果的可靠性，观察到的主要模式（单智能体陷入范式、并行智能体产生多样化方法）在所有 6 次运行中一致出现，保证了结论的稳健性。
    - 任务选择具有代表性：类别结构化解空间、明确的多指标权衡（精确率 vs. 覆盖率），能有效检验搜索行为。
  - 局限：
    - 未进行消融实验（例如改变智能体数量、移除信息共享、采用不同的种子模型），因此无法分解并行性、计算总量、信息可见度等因素的独立贡献，也不能确立“智能体数量”与性能之间的因果或标度关系（作者已明确说明）。
    - 仅在一个任务上实例化，未展示跨任务泛化，尽管框架本身是任务无关的。
    - 未比较其他多智能体协作框架（如 MetaGPT、ChatDev、CoMind 等）在相同任务上的表现，属于自对比实验。

### 6. 论文的主要结论与发现

- **持续改进循环可行**：在两种配置下，排行榜奖励均驱动智能体持续自改进。单智能体最优合格覆盖率从 33.3% 基线提升至 47.8%–57.4%，5 智能体提升至 62.8%–69.4%。
- **并行探索带来方法多样性**：
  - 在所有单智能体运行中（60、124、81 次提交），智能体严格停留在嵌入范式内（BGE‑M3 变体、置信度校准），从未采用非嵌入方法，甚至有一次过早放弃最难类目。
  - 在所有 5 智能体运行中，智能体在几分钟内就独立发现了 TF‑IDF 重排序、字符串相似度匹配等与嵌入方法本质不同的方案，且最终最优方案均来自这些非嵌入方法。
- **搜索行为模式**：单智能体表现为“深度优先但狭窄”，并行智能体表现为“广度优先，牺牲个体精度（P@1 略低）但整体覆盖率更高”，体现了深度与广度的权衡。
- **模式跨运行一致**：中心定性发现（种子单智能体在范式中迭代、并行智能体则逃脱）与智能体数量无关，重复出现，不受计算总量、GPU 争用等混杂因素影响。

### 7. 优点

- **框架的通用性与自主性**：框架不依赖特定任务，可迁移至其他优化问题；实现了“零人类参与”的全自主运行，协调者只处理后勤，是方法论上的亮点。
- **清晰的任务形式化**：将产品匹配转化为选择性预测问题，用精确率门槛和覆盖率最大化的权衡真实反映业务需求，使得实验结果具有实践指导意义。
- **严格的实验基础设施**：对训练/测试数据进行了强有力的隔离，设计了 GPU 资源管理机制和防泄漏检查，保证了实验的公正性和可复现性。
- **深入的行为洞察**：不仅报告指标，还通过案例详细展示了智能体的行为轨迹、方法分化路径和时间线，揭示了单智能体与并行智能体在搜索策略上的定性差异。

### 8. 不足与局限

- **未进行消融实验与因果分析**：无法确定覆盖率的提升究竟是来自并行性、信息共享、额外计算量，还是多个独立探索者的“幸运组合”。作者也承认不能得出智能体数量的因果效应。
- **单一任务验证**：仅在产品目录匹配任务上测试，未在其他信息检索或机器学习任务上验证框架的通用性和有效性。
- **缺乏与多智能体协作框架的直接对比**：仅与简单基线对比，没有将并行竞争机制与其他协作式多智能体框架（如 CoMind、ChatDev 等）进行比较，难以界定该范式的相对优势。
- **模型和设定单一**：所有智能体均使用 Claude Sonnet 3.5，未考察不同底层 LLM 对探索行为的影响；任务时间窗口固定为 5 小时，更长或更短场景未探索。
- **安全与审查未覆盖**：智能体自主生成并执行代码，虽然基础设施防止了相互干扰，但未对代码内容进行人工安全性审计，在真实部署中存在潜在风险。
- **可能存在的模型先验偏差**：种子基线可能导致单智能体行为并非框架内在特性，而是模型对嵌入方法的偏好，作者建议未来通过无种子对照实验来分离这一影响。

（完）
