---
title: "Enhancing Virtual Agents through SLMs and Edge-Computing: An Exploratory Evaluation of Think and Memory Processes"
title_zh: 通过SLMs与边缘计算增强虚拟智能体：对“思考”与“记忆”过程的探索性评估
authors: "Aimilios Hadjiliasi, Louis Nisiotis"
date: 2026-08-13
pdf: "https://arxiv.org/pdf/2608.13420v1"
tags: ["query:agent-memory"]
score: 9.0
evidence: 具身虚拟智能体的记忆过程
tldr: 目前虚拟代理在元宇宙中需具备持久、自适应、上下文感知能力，但认知实现复杂且有技术挑战。本文提出用小型语言模型（SLM）在边缘设备上实现CEAA框架的思考与记忆过程，并开发了边缘虚拟代理网关系统。在NVIDIA Jetson Orin NX上部署不同尺寸Qwen2.5模型，评估路由准确性、内存读取性能和延迟。结果表明SLM驱动的原型系统可高效支撑具身代理认知脑，为沉浸式虚拟世界中的交互体验提供可行方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.13420v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1657, \"height\": 653, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.13420v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 870, \"height\": 388, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.13420v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 785, \"height\": 280, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.13420v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 724, \"height\": 439, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.13420v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 801, \"height\": 338, \"label\": \"Table\"}]"
motivation: 现有虚拟代理认知架构在复杂虚拟世界和边缘环境中实现困难，难以满足持久与上下文感知需求。
method: 基于CEAA框架，在Jetson Orin NX上部署Qwen2.5模型，构建边缘网关并评估Think与Memory过程。
result: 实验显示原型系统在路由准确性、内存读取和延迟方面表现良好，部分实现了CEAA的Think与Memory过程。
conclusion: 边缘部署SLM可支持具身虚拟代理的认知核心，为沉浸式虚拟世界提供高效可行的实现路径。
---

## 摘要
具身智能虚拟智能体被期望在复杂的虚拟世界和元宇宙世界中作为持久、自适应且具备情境感知的实体运行。然而，在此类环境中实现具备认知能力的智能体在概念上和技术上都面临挑战。在众多蓝图和开发方法中，认知具身智能体架构（CEAA）已被开发为一种面向实现的框架，用于架构感知、记忆、推理、规划和具身行动等组件。考虑到边缘计算和生成式AI语言模型的最新进展，本文探索使用小型语言模型（SLMs）来支持所选CEAA组件的边缘化运行，重点关注作为虚拟智能体在交互式虚拟世界中的认知编排与持久性核心过程的“思考”和“记忆”。开发了一个基于边缘的虚拟智能体网关系统，并在NVIDIA Jetson Orin NX上使用不同规模的Qwen2.5模型进行了评估，探索系统处理服务请求和处理记忆驱动对话的能力。一系列仿真实验评估了路由准确性、记忆读取性能和延迟，展示了一个SLM驱动的原型智能体系统，该系统部分实现了所选CEAA过程，以支持具身智能体的开发，使其认知“大脑”能够在沉浸式虚拟世界中高效且情境化地运行以提供交互体验。

## Abstract
Embodied intelligent virtual agents are expected to operate as persistent, adaptive, and context-aware entities within complex virtual and Metaverse worlds. However, implementing cognitively capable agents in such environments is conceptually and technologically challenging. Among a range of blueprints and development approaches, the Cognitive Embodied Agent Architecture (CEAA) has been developed as an implementation-oriented framework for architecting components of perception, memory, reasoning, planning, and embodied action. Considering the recent advances in edge computing and generative AI language models, this paper explores the use of Small Language Models (SLMs) to support edge-based operation of selected CEAA components, focusing on "Think" and "Memory" as processes central to cognitive orchestration and persistence of virtual agents in interactive virtual worlds. An edge-based virtual agent gateway system was developed and evaluated on an NVIDIA Jetson Orin NX using Qwen2.5 models of different sizes, exploring the system's capability to process service requests and handle memory-driven conversations. A series of simulation experiments evaluated routing accuracy, memory-read performance, and latency, demonstrating an SLM-driven prototype agent system that partially implements selected CEAA processes to support the development of embodied agents whose cognitive "brain" can operate efficiently and contextually for interactive experiences in immersive virtual worlds.

---

## 论文详细总结（自动生成）

## 1. 核心问题与研究动机

- 论文聚焦于在复杂虚拟世界 / 元宇宙中实现**具身智能虚拟智能体（embodied intelligent virtual agents）**所面临的认知实现难题。
- 现有虚拟智能体多依赖脚本行为、规则流或符号化游戏 AI，难以做到**持久、自适应、上下文感知**，在实时交互中缺乏真正的认知能力。
- 已有架构如 CEAA（Cognitive Embodied Agent Architecture）提供了感知、记忆、推理、规划、行动等模块化蓝图，但如何在实际边缘环境中运行仍不明确。
- 本文旨在探索用**小型语言模型（SLMs）**在**边缘设备**上部分实现 CEAA 的 **Think（思考）** 与 **Memory（记忆）** 两大核心过程，使虚拟智能体的“认知大脑”能够本地、低延迟、上下文相关地运行。
- 研究问题（RQ）：SLM 能否通过**服务路由**和**结构化记忆处理**在边缘环境中部分操作化 CEAA 的 Think 与 Memory 组件？

## 2. 方法论

- **系统架构**：
  - 开发了一个边缘虚拟智能体网关系统，前端使用 Unity 呈现具身虚拟化身，后端由 NVIDIA Jetson Orin NX 8GB 承载。
  - 语言处理、记忆管理、路由选择和服务分发均在边缘网关完成，Unity 客户端保持轻量。
  - 系统与 CEAA 架构对应：SLM 充当“认知大脑”，执行 Think 和 Memory 的部分功能。

- **Think 实现——服务路由**：
  - 每个 HTTP 请求包含用户提示和会话标识。
  - SLM 首先将交互分类为：对话（conversation）、记忆读取（memory-read）、记忆写入（memory-write）、服务请求（service request）或模糊（ambiguous）。
  - 对服务请求，SLM 返回结构化 JSON，包含所选路由、解释意图、置信度和理由。
  - 路由实验定义 10 条预配置服务路由（编码支持、游戏 AI 指导、玩法机制、3D 生成、图像到 3D 生成、动作生成、对话生成、音效生成、文本到语音、纹理生成），每条路由有名称、描述、示例和路由约束。
  - 路由实验仅评估 SLM 是否选对目标路由，不执行下游生成服务。

- **Memory 实现——结构化写入 / 读取**：
  - 集成 Qwen2.5、LangMem 与 SQLite。
  - 无论交互标签如何，每一轮对话都会被记录到 SQLite，并由 LangMem 使用同一 Qwen2.5 模型提取简洁的持久事实。
  - 事实以 content-keyed 项目存储，包含会话标识、时间戳和元数据；完全重复的事实更新已有条目，非重复的更正则作为新记录保留，旧信息不自动删除。
  - 对 memory-read 请求，网关基于 token 相关性和最近性确定性检索相关事实和最近会话轮次，直接提供给 Qwen2.5 生成回答，不再经过 LangMem 过滤。
  - 记忆准确率反映完整的分类—提取—持久化—检索—生成流水线。

- **模型配置**：
  - 使用 Qwen2.5 系列指令微调模型：0.5B、1.5B、3.0B 三个尺寸。
  - 采用 GGUF 格式、Q4_K_M 量化，通过 llama.cpp 本地服务，上下文长度 4096 token，生成上限 420 token。
  - 无额外训练或微调，均为预训练指令模型直接推理。

## 3. 实验设计

- **路由评估**：
  - 数据集：1,000 条提示，10 条预定义服务路由各 100 条，提示由 LLM 辅助生成并经作者人工审查和修订，确保与协作游戏开发场景相关，每条提示有唯一 ground-truth 路由。
  - 评估指标：总体准确率、宏平均精确率 / 召回率 / F1、无效输出率、超时率、延迟（平均、中位数、P95）。
  - 路由延迟包括请求提交、提示处理、模型推理与解码、输出解析和返回决策，但不包括下游服务执行。

- **记忆评估**：
  - 数据集：250 条提示，其中 125 条事实引入（write）和 125 条配对记忆提问（read），模拟协作虚拟世界游戏开发会话。
  - 提示对覆盖：设计事实、所有权与职责、团队兴趣、任务更新、角色身份、角色行为、关联链接、状态行为、纠正 / 覆盖等类别。
  - 评估指标：端到端读取准确率、正确读取计数（满分 125）、memory-write 标签召回率、service leakage（记忆提示被错误路由到外部服务的次数）、延迟。
  - 评分方式：2026 年 6 月使用 ChatGPT 5.5 High Reasoning 对所有 250 条 memory-read 回答进行固定提示评估，检查预期事实、矛盾、过时信息和不支持内容；随后作者采用模型盲审方式人工复核。

- **对比方法**：
  - 主要对比不同规模的 Qwen2.5 模型（0.5B、1.5B、3.0B）在相同任务上的表现。
  - 未与关键词路由、嵌入相似度、任务特定微调 SLM 等其他方法进行对比（论文在局限性中明确指出）。

## 4. 资源与算力

- **推理硬件**：NVIDIA Jetson Orin NX 8GB，代表资源受限的边缘计算平台。
- **模型**：Qwen2.5-0.5B / 1.5B / 3B-Instruct-GGUF，Q4_K_M 量化。
- **推理框架**：llama.cpp，上下文 4096 token，生成上限 420 token。
- **算力细节**：论文未报告 GPU 型号的具体算力指标（如 TFLOPS）、显存占用、推理吞吐量或功耗。
- **训练细节**：模型为现成指令微调版本，**没有进行额外训练或微调**，因此不涉及训练时长。
- 未提及多 GPU、集群或云资源。

## 5. 实验数量与充分性

- **实验规模**：
  - 路由实验：3 个模型 × 1,000 条提示 = 3,000 次路由推理调用。
  - 记忆实验：3 个模型 × 250 条提示 = 750 次记忆相关交互（其中 125 write + 125 read 每模型）。
  - 合计约 3,750 次端到端推理调用。

- **实验类型**：
  - 包含不同模型规模对比（0.5B vs 1.5B vs 3.0B），属于模型尺寸的消融式比较。
  - 没有针对系统组件（如是否使用 LangMem、是否记录所有会话轮次）的消融实验。
  - 没有多个数据集、多个场景（仅协作游戏开发场景）或多次随机重复。

- **客观性与公平性**：
  - 优点：使用统一的提示集和 ground-truth 标签，人工审查减少标注偏差；LLM-as-judge 结合人工盲审提高评分可靠性；使用宏平均指标避免类别不平衡影响。
  - 局限：数据集为 LLM 辅助生成，可能偏向模型熟悉的表达方式；仅三个同家族模型，无法判断跨模型家族或跨硬件平台的公平性。
  - 总体而言，实验在**小型探索性研究**范围内具有一定的充分性，但不足以支撑广泛推广。

## 6. 主要结论与发现

- **SLM 可支持 CEAA Think 过程**：
  - 1.5B 模型路由准确率 85.4%，宏 F1 84.3%；3.0B 模型准确率 87.7%，宏 F1 87.8%，均表现出良好的路由能力。
  - 0.5B 模型路由准确率仅 29.4%，存在严重的路由坍缩，难以区分相近服务。
  - 语义相似的服务（如图像到 3D 生成与 3D 生成）仍易混淆。

- **SLM 可支持 CEAA Memory 过程**：
  - 3.0B 模型记忆读取准确率 93.6%（117/125），能可靠处理事实回忆、状态行为和纠正。
  - 1.5B 模型 78.4%，0.5B 模型 72.8%，小型模型在纠正处理和精细记忆选择上表现较弱。
  - 记忆读取请求有时被误分类为 memory-write，说明记忆保真度与记忆动作分类是两个相对独立的挑战。

- **准确度—响应性权衡**：
  - 0.5B 延迟最低（路由平均 1504 ms，记忆平均 2025 ms）但不稳定。
  - 1.5B 在路由上提供良好平衡（3349 ms）。
  - 3.0B 记忆表现最强，但延迟显著增加（记忆平均 9693 ms，P95 达 14531 ms），不适合对实时性要求高的交互。

- **实际意义**：
  - 边缘部署 SLM 可以部分实现虚拟智能体的认知“大脑”，支持服务路由和上下文记忆，为持久、自适应、上下文感知的具身智能体提供早期证据。
  - 模型选择应取决于目标功能：低延迟编排可用较小模型，记忆密集或语义复杂任务可用较大模型，或采用混合配置。

## 7. 优点

- **真实边缘硬件评估**：在 Jetson Orin NX 上进行推理，贴近实际部署场景，而非仅云端或高端 GPU。
- **结合架构与实现**：将 CEAA 抽象组件具体映射到 SLM 服务路由和结构化记忆处理，具有工程参考价值。
- **分离 Think 与 Memory 评估**：分别考察路由和记忆过程，便于分析不同认知能力的表现。
- **使用宏平均指标**：在路由类别不平衡的情况下仍能公平反映各服务类别的表现。
- **评分方法较严谨**：LLM-as-judge 与人工盲审结合，降低单一评判的偏差。
- **提供补充材料**：完整提示、服务描述和记忆指令公开在 GitHub，增加可复现性。
- **揭示模型规模与任务的匹配关系**：明确提出准确度—延迟权衡，为后续系统设计提供依据。

## 8. 不足与局限

- **实验场景受限**：
  - 仅在受控测试床中进行，未捕获完整具身用户—代理交互（如感知、行为映射、具身行动、实时 3D 交互均未评估）。
  - 只考察了 CEAA 的 Think 和 Memory 两个过程，未涉及 Reasoner、Planner、行为执行等其他组件。

- **数据偏差风险**：
  - 提示集由 LLM 辅助生成，虽经人工审查，但可能偏向生成式模型易处理的表达方式，不能完全代表真实用户的不可预测行为。
  - LLM-as-judge 评分可能存在潜在偏差，即使结合人工复核也难以完全消除。

- **模型与硬件通用性有限**：
  - 仅评估了 Qwen2.5 三个变体（0.5B、1.5B、3.0B），未覆盖其他 SLM 家族（如 Phi、Gemma、Llama 等）。
  - 仅使用单一 Jetson Orin NX 配置，结果难以推广到其他边缘硬件或云端环境。
  - 未对比关键词路由、嵌入相似度或微调 SLM 等基线方法，无法证明生成式 SLM
