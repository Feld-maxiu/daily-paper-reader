---
title: "WFM: Wiki Foundation Model for Complex Agentic Reasoning"
title_zh: WFM：面向复杂智能体推理的 Wiki 基础模型
authors: "Junnan Dong, Linhao Luo, Senlei Zhang, Gong Chen, Taian Guo, Yifei Yu, Rong Tao, Tao Guo, Qian-Wen Zhang, Siyu An, Ruizhi Qiao, Xing Sun"
date: 2026-09-16
pdf: "https://arxiv.org/pdf/2609.18182v1"
tags: ["query:agent-memory"]
score: 9.0
evidence: 持久的非参数知识、长期记忆与检索增强生成用于智能体推理
tldr: 传统稀疏图表示难以满足复杂智能体对密集语义与持久记忆的需求，行业转向耦合文档与拓扑链接的维基式知识表示。本文提出维基基础模型WFM，构建维基图模式，并设计查询条件注意力聚合与NCCL边界交换协议，兼顾语义表达与分布式效率。在五个长期记忆与多跳推理基准上取得显著性能，训练加速达10.5倍。该方法为大规模商业场景中的智能体原生知识表示与检索提供了新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.18182v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1683, \"height\": 663, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.18182v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1801, \"height\": 817, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.18182v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1713, \"height\": 1164, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.18182v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 870, \"height\": 299, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.18182v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 868, \"height\": 392, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.18182v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1815, \"height\": 680, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.18182v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1579, \"height\": 786, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.18182v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1851, \"height\": 597, \"label\": \"Table\"}]"
motivation: 传统稀疏图表示语义密度低，LLM Wiki虽丰富但现有图编码器学习带来巨大分布式开销，阻碍大规模部署。
method: 形式化维基图模式桥接显式拓扑与连续语义，采用查询条件注意力聚合和注意力方差正则化，并设计NCCL边界交换协议降低通信开销。
result: 在五个长期智能体记忆与多跳推理基准上取得优异性能，分布式训练加速达10.5倍。
conclusion: WFM为智能体原生知识表示与检索提供了可扩展的维基基础模型范式，兼顾性能与部署效率。
---

## 摘要
真实世界智能体从根本上需要持久的非参数化知识来进行动态推理，即长期记忆与检索增强生成。尽管图在提供结构化证据方面已展现出可靠优势，但稀疏图表示天然限制了复杂智能体工作流所需的机器可读性与语义密度。受此局限驱动，整个行业正在经历从传统稀疏图到 LLM Wiki 的范式转变；LLM Wiki 是一种智能体原生知识表示，将密集文档上下文与包含多层拓扑链接的 markdown 文件相结合。然而，对此类丰富语义进行参数化时，使用传统稀疏图嵌入难以编码密集文本上下文。此外，用现有图编码器学习 LLM Wiki 可能会使分布式系统开销不堪重负，从而阻碍大规模商业场景中的部署。为此，我们提出一种新颖的 Wiki 基础模型范式，即 WFM，专为可扩展的智能体原生表示与检索而设计。具体而言，(i) 我们形式化了一种 Wiki Graph schema，将细粒度结构与密集上下文无缝桥接，在连续语义之外保持显式拓扑；(ii) 设计了一种查询条件化的注意力聚合机制，用于丰富的 wiki 消息传递，并引入显式注意力方差正则化；(iii) 我们设计了一种基础设施级 NCCL 边界交换协议，提升静态分区索引并利用固定形状的 GPU 到 GPU 集合通信，绕开 CPU 序列化与内存拷贝开销。在五个长期智能体记忆与多跳推理基准上的广泛评估表明，WFM 性能卓越，同时在分布式集群上实现了 10.5 倍的训练加速。

## Abstract
Real-world agents fundamentally require persistent non-parametric knowledge for dynamic reasoning, i.e., long-term memory and retrieval-augmented generation. While graphs have shown reliable advantages in providing structured evidence, the sparse graph representations naturally restrict machine readability and semantic density required for complex agentic workflows. Driven by this limitation, the entire industry is witnessing a paradigm shift from traditional sparse graphs to LLM Wiki, an agent-native knowledge representation that couples dense document contexts with markdown files containing multi-layered topological linkages. However, parameterizing such rich semantics is challenging to encode dense textual contexts using traditional sparse graph embeddings. Moreover, learning LLM Wiki with existing graph encoders could overwhelm distributed system overheads that hinder deployment in large-scale commercial scenarios. To this end, we propose a novel paradigm Wiki Foundation Model, i.e., WFM, tailored for scalable, agent-native representation and retrieval. Specifically, (i) we formalize a Wiki Graph schema that seamlessly bridges fine-grained structures with dense contexts, maintaining explicit topologies alongside continuous semantics; (ii) A query-conditioned attentive aggregation is tailored for rich wiki message passing and explicit attention variance regularization; (iii) We engineer an infrastructural NCCL boundary exchange protocol that hoists static partition indices and leverages fixed-shape GPU-to-GPU collectives, bypassing CPU serialization and memory copy overheads. Extensive evaluations across five long-term agent memory and multi-hop reasoning benchmarks demonstrate the remarkable performance of WFM, while achieving a 10.5 times training acceleration on distributed clusters.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **研究背景与动机**：真实世界智能体需要持久、非参数化的知识支持动态推理，即长期记忆与检索增强生成（RAG）。传统 RAG 采用分块向量检索，会切断上下文关联；GraphRAG 虽能利用实体关系进行结构化推理，但稀疏三元组表示会丢失密集文本语义，机器可读性和语义密度不足，难以支撑复杂智能体工作流。
- **范式转变**：行业正从稀疏图转向 **LLM Wiki**，一种智能体原生的知识表示，将密集文档上下文与含多层拓扑链接的 Markdown 文件耦合。
- **核心问题**：传统稀疏图嵌入难以编码 LLM Wiki 的密集文本语义；现有图编码器直接用于 LLM Wiki 会带来巨大分布式系统开销，阻碍大规模商业部署。
- **整体含义**：论文提出 **Wiki Foundation Model（WFM）**，面向可扩展的智能体原生表示与检索，将图构建、表示学习和分布式系统执行统一为垂直一体化框架，同时解决语义建模与基础设施效率问题。

## 2. 论文提出的方法论

- **Wiki Graph 构建与双空间初始化**：
  - 将 LLM Wiki 建模为混合图 \(W=(E_w,R_w,D)\)，节点集为实体 \(E_w\) 与文档/段落 \(D\)。
  - 两类边：实体—实体类型化关系边，以及实体—段落跨层链接边，使段落直接参与消息传递而不丢失文本语义。
  - 实体和关系通过可训练查找表映射到结构空间 \(\mathbb{R}^{d_s}\)；文档通过预训练语言模型 \(T(\cdot)\) 编码到语义空间 \(\mathbb{R}^{d_t}\)，再用线性投影 \(W_p\) 映射到统一传播维度。

- **注意力聚合图基础模型（Attentive GFM）**：
  - 关系感知注意力打分：\(\pi(v,r,u)=w_a^T \tanh(W_r h_u + e_r - W_r h_v)\)。
  - 对所有类型化邻居做 Softmax 归一化，得到注意力系数 \(\alpha(v,r,u)\)。
  - 双空间消息聚合：\(m_v = \sum \alpha(v,r,u)(W_v h_u + e_r)\)。
  - 使用 **Bi-Interaction 聚合器**更新节点：加性分支保留独立信息，乘性分支强调状态一致性。

- **迭代检索与智能体自反思**：
  - 设定反思预算 \(B\)，每轮用当前查询编码与文档状态相似度检索 TopK 证据。
  - 证据跨轮累积；LLM 同时输出候选答案、后续查询和完成标记 \(f^{(t)}\in\{\text{Continue},\text{Final}\}\)。
  - 若标记为 Final 或达到预算 \(B\) 则停止，实现自适应提前终止。

- **多任务损失与预热课程**：
  - **拓扑结构损失 \(L_{topo}\)**：TransE 风格的边际排序损失，保留实体关系边界。
  - **文档对齐损失 \(L_{align}\)**：InfoNCE 对比损失，对齐实体与文档表示。
  - **注意力方差正则化损失 \(L_{var}\)**：对注意力 logit 方差设置下界 \(\epsilon\)，用 hinge 惩罚低方差，防止注意力均匀坍塌和梯度锁死。
  - 总损失：\(L_{total}=L_{topo}+\lambda_1 L_{align}+\lambda_2 L_{var}+\lambda_3 \|\Theta\|_2^2\)。
  - **预热课程**：第一阶段冻结 GFM，仅训练投影和查找表在 \(L_{align}\) 下；第二阶段全参数联合优化 \(L_{total}\)。

- **NCCL 原生边界交换协议**：
  - 将静态图分区索引离线预处理，边界布局填充为固定形状张量。
  - 使用 NCCL AllToAll 进行 GPU 到 GPU 集合通信，绕过 CPU 序列化与 host-to-device 内存拷贝。
  - 将每步延迟从 2.40s 降至 0.23s，实现 10.5 倍端到端训练加速。

## 3. 实验设计

- **数据集与场景**：
  - **多跳开放域 QA**：HotpotQA、2WikiMultihopQA、MuSiQue，各取 1,000 条 held-out 问题；对应语料 chunk 数分别为 9,990、6,642、11,694。
  - **长期记忆 QA**：RHELM 1M 七类问题（事实、时间、聚合、幻觉、误导、附件、混合）；PersonaMem 1M（20 个人设、2,674 个问题）。
- **评估指标**：
  - 检索质量：Recall@k（k=2/5/10/20）。
  - 端到端质量：LLM 判官打分的 ACC；多跳 QA 分 Open 模式与 Reject 模式。
- **对比方法**：
  - 多跳 QA：Zero-shot LLM、Native RAG、RAPTOR、E2GraphRAG、LightRAG、GraphRAG、HippoRAG1/2、HippoRAG-IRCOT、GFM-RAG、Youtu-GraphRAG。
  - 长期记忆：Native RAG、LightRAG、GraphRAG、Youtu-GraphRAG、GFM-RAG、A-mem、MemoryOS、LightMem、全上下文 Zero-shot。
- **实现配置**：DeepSeek V4 Flash 生成答案，DeepSeek V4 Pro 判断答案；嵌入模型 all-MiniLM-L6-v2；检索深度 20；WFM 使用 3 层图注意力、\(\epsilon=0.05\)、\(\lambda_2=0.1\)、最大反思预算 \(B=4\)。

## 4. 资源与算力

- 论文**未明确给出具体 GPU 型号、GPU 数量、总训练时长或完整硬件规模**。
- 仅提到在“多 GPU 集群”上训练，通信涉及 NVLink 或 InfiniBand，并报告了每步延迟从 2.40s 降至 0.23s、训练加速 10.5 倍。
- 因此，从算力可复现性角度看，硬件配置信息不足。

## 5. 实验数量与充分性

- **实验数量较丰富**：
  - 覆盖 5 个 benchmark：3 个多跳 QA + 2 个长期记忆 QA。
  - 多组 Recall@k 与 ACC 指标。
  - 超过 10 种基线方法对比。
  - 消融实验：普通实体图、DistMult 聚合器、去掉 \(L_{var}\)、去掉预热课程、去掉自反思、去掉最终标记强制执行全部轮次。
  - 参数分析：图注意力层数 1–6、反思预算 1–6、\(\epsilon\)–\(\lambda_2\) 网格敏感性。
- **充分性总体较好**，但存在可靠性隐患：论文在实现细节中明确写道，“新完成的单元格和诊断扫描是构建的规划值，需要在实际运行中验证后才能对外使用”。这意味着部分表格数据或诊断结果可能不是完全实测，影响结果可信度。
- **公平性**：多数方法使用相同数据划分、嵌入模型和检索深度；但 DistMult 基线为可运行而限制了邻域规模，未限制时会 OOM，因此该对比并非完全同等条件。

## 6. 论文的主要结论与发现

- WFM 在三个多跳 QA 数据集上取得最高 Open/Reject 准确率：HotpotQA 89.6/84.3，2Wiki 90.2/82.4，MuSiQue 69.8/52.6；相对最强基线提升显著，且 Reject 模式增益更大，说明主要提升来自证据增强而不是参数化知识修补。
- 检索召回方面，WFM 在 HotpotQA 与 2Wiki 的 Recall@20 达到 93.20 和 90.15；相比 GFM-RAG，在三个数据集上 Recall@20 分别提升 11.82、17.52、26.67 点。
- 长期记忆任务上，WFM 在 PersonaMem 总体 ACC 58.49，RHELM 52.17，分别超过最强基线 8.39 和 5.37 点；即使去掉自反思，仍超过最强基线。
- 消融证明：Wiki Graph 中的密集文本节点不可替代；注意力方差正则化和预热课程对稳定训练有重要作用；自适应停止能以更低成本接近全预算效果。
- 系统层面，NCCL 边界交换协议实现 10.5 倍训练加速，展示了基础设施协同设计的价值。

## 7. 优点

- **范式创新**：将 LLM Wiki 引入图基础模型，形式化了稀疏图到文本增强拓扑的转变。
- **联合建模**：通过

- **联合建模**：通过双空间表示与消息传递同时保留结构信息与文本语义，避免稀疏三元组的信息损失，使图基础模型能够直接处理 LLM Wiki 的密集上下文。
- **系统协同设计**：NCCL 原生边界交换协议绕过 CPU 与 host-to-device 拷贝，显著降低通信开销，使分布式训练从不可行变为可行，体现了算法与系统联合优化的价值。
- **训练稳定性设计**：注意力方差正则化与预热课程有效缓解注意力坍塌和梯度锁死，提升了深层图注意力网络的训练稳定性，对复杂图结构具有通用借鉴意义。
- **实验全面性**：覆盖多跳开放域 QA 与长期记忆 QA 两类任务，对比十余种基线，并进行消融与参数敏感性分析，验证了方法在不同场景下的有效性。

## 8. 缺点与局限性

- **硬件与可复现性信息不足**：论文未明确给出 GPU 型号、数量、总训练时长等关键资源信息，仅报告相对加速，导致外部难以精确复现实验环境。
- **部分结果可信度存疑**：论文实现细节中明确标注“新完成的单元格和诊断扫描是构建的规划值，需要在实际运行中验证后才能对外使用”，这意味着部分表格数据或诊断结果可能并非完全实测，影响结论可靠性。
- **对比公平性有限**：DistMult 基线因内存溢出限制了邻域规模，未受限时会 OOM，因此该对比并非完全同等条件，削弱了部分消融结论的说服力。
- **对嵌入模型依赖较强**：文档节点编码依赖预训练语言模型 all-MiniLM-L6-v2，未探讨不同嵌入模型对性能的影响，且该轻量模型可能不足以充分表征复杂语义。
- **缺乏大规模验证**：实验所用语料 chunk 数均在 1 万左右，实体规模有限，未验证在百万级实体或工业级 Wiki 图上的扩展性与稳定性。
- **反思机制开销未充分评估**：自适应反思虽能提前终止，但论文未报告端到端推理延迟或 token 成本，实际部署中 LLM 多次调用可能带来显著开销。
- **未讨论错误传播**：图构建依赖实体链接与关系抽取，若上游抽取存在噪声，对下游表示学习与检索的影响未进行分析。

## 9. 总结与展望

论文针对 LLM Wiki 这一智能体原生知识表示，提出了 Wiki Foundation Model（WFM），将图构建、双空间表示学习、注意力聚合与分布式训练统一为垂直一体化框架。其核心贡献在于同时解决语义建模与基础设施效率问题，通过文本增强拓扑、双空间消息传递、注意力方差正则化与 NCCL 边界交换协议，在多跳 QA 与长期记忆任务上取得了显著提升。该方法为智能体长期记忆与检索增强生成提供了一种可扩展的图基础模型范式。

然而，论文在硬件配置透明性、部分结果实测性、大规模验证等方面仍存在不足，且部分对比公平性有限。未来工作应补充完整实验细节与代码开源，在大规模真实 Wiki 图上验证扩展性，并进一步优化反思机制的计算开销。总体而言，WFM 为 LLM Wiki 的表示与检索提供了有价值的探索，但距离工业级可信部署仍需更多实证支持。

（完）
