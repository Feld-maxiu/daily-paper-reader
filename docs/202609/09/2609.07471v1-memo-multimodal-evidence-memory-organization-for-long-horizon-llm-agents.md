---
title: "MEMO: Multimodal Evidence Memory Organization for Long-Horizon LLM Agents"
title_zh: MEMO：面向长程LLM智能体的多模态证据记忆组织
authors: "Xian Gao, Jinpeng Wang, Jiacheng Ruan, Guangyu Cao, Ting Liu, Yuzhuo Fu"
date: 2026-09-07
pdf: "https://arxiv.org/pdf/2609.07471v1"
tags: ["query:agent-memory"]
score: 9.0
evidence: 长时程LLM智能体的多模态证据记忆组织
tldr: 长程 LLM 智能体依赖外部记忆存储与复用信息，但记忆不断累积与有限上下文之间存在根本矛盾，文本读出保真却线性竞争，视觉读出可布局却损失细节。MEMO 训练证据提取器筛选相关记忆块，并由查询条件记忆管理器为每个证据单元分配合适的文本、视觉或双通道载体及布局，再生成文本包与视觉页。在 HotpotQA、2WikiMultiHopQA、LoCoMo、ALFWorld 四个基准上，MEMO 用更少记忆令牌实现更高下游任务性能，展示了多模态证据组织对受限预算下工作记忆的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.07471v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 866, \"height\": 717, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.07471v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1785, \"height\": 728, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.07471v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1657, \"height\": 1304, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.07471v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 673, \"height\": 261, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.07471v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1809, \"height\": 1304, \"label\": \"Table\"}]"
motivation: 长程LLM智能体的记忆读出需在有限上下文下平衡文本保真与视觉结构化优势，现有方法难以兼顾。
method: MEMO训练证据提取器筛选相关记忆块，查询条件记忆管理器分配文本/视觉/双通道载体和布局，再由确定构建模块生成文本包和视觉页。
result: 在HotpotQA、2WikiMultiHopQA、LoCoMo、ALFWorld四个基准上，MEMO以更少记忆令牌提升下游任务性能并构建更有效工作记忆。
conclusion: MEMO验证了多模态证据组织能高效利用受限上下文，为长程智能体记忆管理提供了有效方案。
---

## 摘要
长时间运行的LLM智能体依赖外部记忆来存储和重用超出单个上下文窗口的信息，但交互轨迹的持续积累与有限的上下文容量之间存在根本性张力。因此，智能体记忆的关键挑战不仅在于检索相关记录，还在于在给定预算下选择必要证据并以合适模态加以组织。现有记忆读取方法主要采用文本或视觉形式。文本保真度高，但其线性token表示使得不同重要性的内容以几乎统一的单元成本竞争有限上下文。视觉读取将文本渲染成类似文档的图像，可利用二维布局展现结构并强调关键信息，但在渲染和压缩过程中可能丢失细粒度细节。为解决这一问题，我们提出MEMO，一种面向LLM智能体的多模态证据记忆组织方法。MEMO首先使用训练好的证据提取器选择相关记忆块，并形成带有来源信息和呈现需求的证据单元。训练好的以查询为条件的记忆管理器将每个单元分配给文本、视觉或双通道载体，并选择与证据结构匹配的布局。随后，确定性记忆构建模块生成文本包和视觉页面。记忆管理器利用离线读取器的反馈进行训练，该读取器衡量引导记忆计划的效用，从而使保留和呈现决策与下游使用保持一致。我们在HotpotQA、2WikiMultiHopQA、LoCoMo和ALFWorld四个基准上，使用多个读取器后端对MEMO进行评估。结果表明，MEMO以更少的记忆token更高效地呈现记忆，提升下游任务性能，并在受限预算下构建更有效的工作记忆。

## Abstract
Long-running LLM agents rely on external memory to store and reuse information beyond a single context window, yet there is a fundamental tension between the continuous accumulation of interaction trajectories and the limited context capacity. The key challenge in agent memory is therefore not only to retrieve relevant records, but also to select necessary evidence under a given budget and organize it in an appropriate modality. Existing memory readout methods mainly use textual or visual forms. Text preserves high fidelity, but its linear token representation makes contents with different importance compete for the limited context at nearly uniform unit cost. Visual readout renders text into document-like images, which can use two-dimensional layouts to expose structure and emphasize key information, but it may lose fine-grained details during rendering and compression. To address this issue, we propose MEMO, a multimodal evidence memory organization method for LLM agents. MEMO first uses a trained evidence extractor to select relevant memory blocks and form evidence units with source information and presentation requirements. A trained query-conditioned memory manager assigns each unit to a textual, visual, or dual-channel carrier and selects a layout that matches the evidence structure. A deterministic memory construction module then generates the textual package and visual pages. The memory manager is trained with feedback from an offline reader that measures the utility of the guided memory plan, so that retention and presentation decisions align with downstream usage. We evaluate MEMO on four benchmarks, HotpotQA, 2WikiMultiHopQA, LoCoMo, and ALFWorld, with multiple reader backends. The results show that MEMO presents memory more efficiently with fewer memory tokens, improves downstream task performance, and builds more effective working memory under constrained budgets.

---

## 论文详细总结（自动生成）

# MEMO 论文总结

## 1. 论文的核心问题与整体含义

- **研究背景**：长程 LLM 智能体需要外部记忆存储超出单个上下文窗口的信息，但交互轨迹不断累积，与有限的上下文容量之间存在根本矛盾。
- **核心问题**：记忆读取（memory readout）不仅需要检索相关记录，更需要在给定的 token 预算下选择必要证据，并以合适的模态进行组织。
- **现有方法局限**：
  - 文本记忆保真度高，但线性 token 表示使得不同重要性的内容以几乎相同的单位成本竞争有限上下文。
  - 视觉记忆通过二维布局强调结构和关键信息，但在渲染与压缩过程中可能丢失细粒度语义细节。
- **整体含义**：单一模态无法同时兼顾信息保真、结构表达与资源效率。MEMO 旨在通过多模态证据组织，在证据单元粒度上联合决定“保留什么”和“如何呈现”，从而在受限预算下构建更高效的工作记忆。

## 2. 论文提出的方法论

- **总体流程**：
  1. 证据提取器从原始记忆中定位与查询相关的内容，形成可溯源的证据单元。
  2. 记忆管理器为每个证据单元分配呈现方式（文本、视觉、双通道或删除），并规划视觉布局。
  3. 确定性构建模块生成文本包和视觉页，统一满足预算约束 \(C_{\text{text}} + C_{\text{vis}} \le B\)。

- **查询条件证据提取**：
  - 输入：查询 \(q\)、记忆记录 \(R\)、预算 \(B\)。
  - 输出：相关源标识符 \(S\) 和字符级关键跨度 \(H\)。
  - 证据单元定义为 \(u_i = (id, x_i, h_i, s_i, c_i)\)，其中 \(x_i\) 为内容，\(h_i\) 为关键跨度，\(s_i\) 为来源，\(c_i\) 为保留优先级规则。
  - 关键跨度周围的局部上下文以窗口宽度 \(w\) 保留，形成最小记忆操作对象。
  - **训练方式**：利用基准数据集的原生证据标注或确定性弱标签进行监督微调（SFT），优化目标见式（2）。

- **联合证据选择与呈现**：
  - 动作集合：\(\{\text{text}, \text{image}, \text{dual}, \text{drop}\}\)。
  - 文本动作保留精确语义锚点；图像动作利用视觉结构表达时间、表格、分组等关系；双通道同时保留文本锚点和视觉结构；删除动作省略低价值证据。
  - 视觉模板包括卡片、时间线、表格、检查清单、列布局等，根据证据语义选择合适的布局。

- **记忆管理器训练**：
  - 对同一查询和证据，构造反事实候选（每个单元分别尝试 text / image / dual / drop），由冻结读取器评估候选记忆的答案质量与资源消耗。
  - 剔除超出预算的候选；在答案质量相近时优先选择资源消耗更少的候选。
  - 最佳动作作为标签，置信度来自最佳与次佳动作之间的差距。
  - 视觉模板标签根据候选方案的答案质量确定，训练目标为 SFT（式（4））。

- **工作记忆构建**：
  - 文本包 \(T\)：由 text 或 dual 单元组成，保留来源标识符和关键措辞。
  - 视觉页 \(V\)：由 image 或 dual 单元组成，根据模板偏好渲染为结构化页面。
  - 最终将查询、文本包和视觉页一起交给冻结的多模态读取器生成答案。

## 3. 实验设计

- **数据集与场景**：
  - HotpotQA、2WikiMultiHopQA：多跳文档问答。
  - LoCoMo：长对话记忆。
  - ALFWorld：具身交互轨迹记忆，评估下一步动作预测。
- **基线方法**：
  - 直接控制：No memory、Text-only、Visual-only。
  - 检索基线：BM25。
  - 文本记忆学习基线：Mem-α、MemAgent。
  - 视觉记忆基线：AgentOCR、MemOCR。
- **读取器后端**：InternVL3.5-8B、Qwen3-VL-32B、gpt-5.4-mini，均为冻结模型。
- **预算设置**：
  - 128-token 严格预算：所有方法必须满足文本 token + 视觉 token ≤ 128。
  - 无预算设置：不施加方法级限制，但受 4,096-token 上下文窗口约束。
- **评估指标**：归一化精确匹配（EM）和 token 级 F1。

## 4. 资源与算力

- 训练组件（证据提取器和记忆管理器）使用 Qwen2.5-1.5B-Instruct 作为骨干。
- 记忆管理器训练时使用 InternVL3.5-4B-Instruct 作为冻结读取器提供反馈。
- 实验在 NVIDIA A100 GPU 上完成。
- **论文未明确说明 GPU 数量、训练时长、批量大小等具体算力细节**，仅提及使用了 A100 GPU。

## 5. 实验数量与充分性

- **主要实验**：两个预算设置（128-token 与无预算）× 三个读取器，共 6 个大表，每个表覆盖 4 个数据集和 9 种方法。
- **消融实验**：1 个组件消融表，分别比较证据提取器和记忆管理器的作用。
- **充分性**：实验覆盖多数据集、多读取器、不同预算条件，并包含消融分析，整体较为全面。
- **公平性**：所有方法使用相同的冻结读取器，并遵守相同的预算约束；无预算设置也保证所有源记忆可放入 4,096-token 窗口，避免了因截断造成的不公平。
- **潜在局限**：
  - 仅使用三个读取器，代表性有限。
  - 记忆管理器训练时的读取器为 InternVL3.5-4B，与评估中的 InternVL3.5-8B 同属一个系列，可能对该系列存在一定偏好。
  - 在 LoCoMo 和 ALFWorld 上，MEMO 并非所有单项指标均第一，个别指标被其他基线超过。

## 6. 论文的主要结论与发现

- 在 128-token 严格预算下，MEMO 在三个读取器上均取得最佳 Overall EM 和 F1。
  - 示例：Qwen3-VL-32B 在 2Wiki 上，MEMO F1 为 73.91，而 text-only 为 56.26，visual-only 为 35.89。
- 在无预算设置下，MEMO 平均仅使用 83.93 tokens，比 MemAgent 少 50.5%–50.7%，比 Text-only 少 66.9%，同时 Overall EM/F1 排名第一或第二。
- 消融实验显示：
  - 仅启用证据提取器：EM +6.07，F1 +8.85，tokens 从 126.22 降至 70.12（减少 44.4%）。
  - 再启用记忆管理器：EM 进一步提升 4.22，F1 +2.53，tokens 增至 81.07，但仍远低于未启用时的 126.22。
- 结论：证据级多模态组织有效利用有限上下文，在保持或提升性能的同时显著降低记忆 token 消耗。

## 7. 优点

- **细粒度证据单元**：以字符级关键跨度为基础，保留溯源信息和局部上下文，相比段落或整块记忆更精细。
- **联合优化**：在同一框架内联合决策证据选择、模态分配和视觉布局，避免多阶段间的信息损失。
- **跨读取器鲁棒性**：在三个不同系列/规模的 VLM 上均表现出优势，降低对特定后端的依赖。
- **高效压缩**：在无预算设置下以极低 token 消耗保持接近最佳的性能，体现“不随意消耗 token”的原则。
- **可解释性**：证据单元保留来源链接，动作和模板选择有对应标签，便于分析。
- **消融清晰**：实验明确区分证据提取器和记忆管理器的贡献，证明两者互补。

## 8. 不足与局限

- **算力信息不完整**：未报告 GPU 数量、训练时长及超参数，复现性受限。
- **训练数据依赖性**：证据提取器依赖基准原生标注或弱标签，在缺乏高质量标注的新领域可能退化。
- **读取器偏差风险**：记忆管理器使用 InternVL3.5-4B 训练，可能对 Intern

VL 系列存在偏好；评估中虽包含 Qwen3-VL 和 gpt-5.4-mini，但仍不能完全排除同系列带来的优势。

- **视觉模板适应性有限**：MEMO 的视觉表达依赖预定义模板（如卡片、时间线、表格、列布局等），对高度开放、非结构化或需要新型布局的证据可能不够灵活；渲染质量也受 VLM 视觉理解能力的制约。
- **证据提取误差会向下传播**：记忆管理器在证据提取器给出的证据单元上做选择，若提取阶段遗漏关键跨度或误标来源，后续即使动作选择正确也难以恢复，论文未系统分析该误差传播影响。
- **工作记忆以单查询为主**：MEMO 构建的是查询级工作记忆，对多轮交互、跨查询复用或增量更新场景，论文未展示持续维护和更新机制。
- **训练成本偏高**：记忆管理器训练需要为每个证据单元构造多个反事实候选并调用冻结读取器评估，虽然读取器冻结，但候选生成与评估过程仍然较昂贵；推理阶段也涉及视觉渲染和多模态读取，未报告端到端延迟。
- **评估指标侧重结果匹配**：主要使用 EM 和 token 级 F1，缺少对推理链质量、证据充分性、视觉布局可读性或人类可理解性的直接评估。

（完）
